# 진행 상황 (PROGRESS)

- 마지막 갱신: **2026-08-28**
- 만드는 것: 한국어나 일상어로 물어도 arXiv 논문을 찾아 주는 검색 서비스
- 다른 문서: 큰 그림 [PLAN.md](PLAN.md) · 모듈 구조 [MODULE_SPECIFICATION.md](MODULE_SPECIFICATION.md) ·
  문제 기록 [ISSUE.md](ISSUE.md) · **산출물 기록 [ARTIFACTS.md](ARTIFACTS.md)** ·
  평가 실행법 [../evaluation/README.md](../evaluation/README.md)

| 말 | 뜻 |
|---|---|
| Recall@10 | 상위 10편 안에 질문을 만든 그 논문이 들어온 비율 |
| nDCG@10 | 상위 10편이 얼마나 쓸모 있는가. 등급 정답지로 잼 |
| 후보 상한 | 재정렬 전 후보 안에 정답이 들어 있는 비율. 재정렬로 이 값을 넘을 수 없음 |
| 회수율 | 후보 안에 있던 정답 중 실제로 상위 10편까지 올라온 비율 |

---

## 1. 지금 상태

### 시험용 342문항 성능

| 구성 | Recall@10 | nDCG@10 |
|---|---|---|
| **미세조정 색인 + 순위 합치기 3:1** ← **지금 서비스 구성 (2026-08-28 채택)** | **0.658** | 잴 수 없음 |
| 미세조정 색인만 (2026-08-27 측정) | 0.629 | 잴 수 없음 |
| 검색어 2개(번역 + 가상 초록) 합치기, 깊이 100 (옛 서비스 구성) | 0.617 | 0.521 |
| 번역만, 깊이 100 (옛 기준선) | 0.608 | 0.512 |
| 변환기(dpo) + 두 채널 | 0.570 | 0.442 |
| 로컬 의미 검색 단독 (번역 없음) | 0.553 | 0.484 |
| 변환기 없음 + 두 채널 | 0.553 | 0.458 |
| arXiv 단독 | 0.389 | 0.251 |

짝지은 부트스트랩 검정:

| 비교 | Recall@10 | nDCG@10 |
|---|---|---|
| 번역 추가 (로컬 단독 대비) | **+0.056 (p=0.001)** | **+0.027 (p<0.001)** |
| **검색어 2개 합치기 (번역만 대비)** | +0.009 (p=0.574, 판정 불가) | +0.009 (**p=0.050, 판정 불가**) |
| 변환기(dpo) + arXiv 채널 추가 | +0.018 (p=0.101, 판정 불가) | **−0.043 (p<0.001, 나빠짐)** |

목표는 Recall@10 0.70 이고 지금 **0.658** 임. 난이도별로 easy 0.904 · medium 0.737 ·
hard 0.333, 언어별로 한국어 0.661 · 영어 0.655 임.

> **2026-08-28 채택 근거를 정확히 적어 둠.** 옛 구성 대비 전체 +0.041 인데 **p=0.085 로
> 유의성을 확보하지 못했음**(신뢰구간 [−0.003, +0.085]). 개발용에서는 +0.060(p=0.003)
> 이었고 시험용에서 방향과 크기가 재현됐으나 342문항으로는 이 크기를 잡아내기에 모자람.
> **채택 근거는 성능이 아니라 ⓐ 여섯 무리 중 유의미하게 나빠진 것이 하나도 없고
> ⓑ 한국어가 유의미하게 좋아졌다는 것임**(+0.070, p=0.045). 2026-08-16 에 옛 구성을
> 채택할 때와 같은 형태의 근거임. 자세한 것은 [RESULTS.md](RESULTS.md) 참고.

> **검색어 2개 합치기의 이득은 시험용에서 확정되지 않았음.** 개발용에서는 nDCG 가
> +0.009(p=0.040)로 유의미했으나 시험용에서 p=0.050 으로 경계에 걸림. 두 지표 모두 방향은
> 개발용과 시험용에서 일치하지만(둘 다 +0.009), **"좋아졌다"고 단정하면 안 됨.**
> 이 구성을 채택한 근거는 성능 향상이 아니라 ⓐ 두 지표 어느 쪽도 나빠지지 않았고
> ⓑ 응답 시간이 21.1초에서 15.1초로 줄었으며 ⓒ 옛 구성이 코드상 고장나 있었다는 것임(#39).

### 검색 모델 미세조정 결과 (2026-08-27, ISSUE #53)

임베딩 모델을 미세조정해(`models/retriever-ft`) 71만 편을 다시 색인함
(`data/embeddings/cs2021-ft`). **개발용에서는 세 지표 모두 좋아졌으나 시험용에서
확정되지 않았음.**

개발용 348문항 — 검색어는 저장된 것을 다시 써서 색인만 바꿈:

| 지표 | 지금 색인 | 미세조정 색인 | 차이 | p | 판정 |
|---|---|---|---|---|---|
| Recall@10 | 0.575 | **0.618** | +0.043 | 0.031 | **유의미** |
| nDCG@10 | 0.662 | **0.684** | +0.022 | 0.002 | **유의미** |
| 상위 10편 중 등급 2 이상 편수 | 6.057 | **6.287** | +0.230 | 0.015 | **유의미** |

시험용 342문항 — **전부 판정 불가**:

| 무리 | 지금 색인 | 미세조정 색인 | 차이 | p |
|---|---|---|---|---|
| 전체 | 0.617 | 0.629 | +0.012 | 0.632 |
| easy | 0.930 | 0.886 | −0.044 | 0.155 |
| medium | 0.658 | 0.719 | +0.061 | 0.187 |
| hard | 0.263 | 0.281 | +0.018 | 0.734 |

**1차 검색은 좋아졌는데 재정렬이 먹고 있음.** 시험용 hard 층 후보 상한이 0.395 에서
0.500 으로 올랐으나(p=0.054), 회수율이 0.667 에서 0.561 로 떨어져 최종값에 안 나타남.
편수로 보면 후보에 정답이 든 문항이 45편에서 57편으로 12편 늘었는데 상위 10편에 올린
것은 30편에서 32편으로 2편만 늘었음. **늘어난 12편 중 10편을 재정렬이 버렸음(#41).**

**결정: 서비스 색인 교체는 보류.** 색인과 모델은 파일로 남겨 둠. 2026-08-16 에 검색어
2개 합치기를 채택할 때의 근거가 "두 지표 어느 쪽도 나빠지지 않았음" 이었는데, 이번에는
easy 가 시험용 −0.044, 개발용 만족 편수 −0.310(p=0.044)으로 나빠지는 방향이라 그 근거가
성립하지 않음.

> **시험용 nDCG 는 이번 판정에서 쓰지 않았음.** `grades_test.jsonl` 을 안 넓혀서 등급
> 보유율이 지금 색인 0.476 · 미세조정 색인 0.304 임. 양쪽 다 절반 이상이 0점으로 세어짐.

> **개발용 nDCG 절대값이 바뀌었음.** 등급 정답지를 1,233쌍 넓혀서 지금 서비스의 값이
> 0.670 에서 **0.662** 가 됐음. 성능이 변한 것이 아니라 IDCG 가 커진 것임.
> **2026-08-18 값과 2026-08-27 값을 섞어 비교하면 안 됨.**

### 개발용 만족도 — 넓힌 등급 정답지로 다시 잼 (2026-08-18, ISSUE #42)

짝당 판정 편수를 20편에서 71편으로 넓힌 뒤의 nDCG@10 임. **확장 전 값과 직접 비교하면
안 됨** — 판정 편수가 늘면 IDCG 가 커져 절대값이 내려감. 구성끼리만 비교할 것.

| 구성 | 옛 정답지 | **새 정답지** | 등급 보유율 |
|---|---|---|---|
| **지금 서비스 (재정렬 후)** | 0.525 | **0.670** | 1.000 |
| 후보 깊이 200 | 0.505 | 0.670 | 1.000 |
| 번역문 로컬 (재정렬 없음) | 0.514 | 0.651 | 1.000 |
| 원본질문 로컬 (재정렬 없음) | 0.484 | 0.613 | 1.000 |
| 지금 서비스 (재정렬 전, 융합 후) | 0.532 | 0.592 | 1.000 |
| 변환기 없음 | 0.457 | 0.578 | 0.916 |
| 변환기 dpo | 0.436 | 0.553 | 0.807 |

짝지은 부트스트랩 (등급 없는 논문을 0점으로 세든 목록에서 빼든 **같은 값**이 나옴):

| 비교 | nDCG@10 |
|---|---|
| **재정렬 적용 (재정렬 전 대비)** | **+0.078 (p<0.001)** |
| **번역 적용 (원본질문 대비)** | **+0.038 (p<0.001)** |
| 후보 깊이 200 (깊이 100 대비) | +0.001 (p=0.902, 판정 불가) |

> **2026-08-17 에 "재정렬이 만족도를 해친다" 고 적었던 것은 틀렸음.** 등급 정답지가
> 좁아서 생긴 편향이었음. 재정렬은 Recall@10 과 nDCG@10 양쪽에서 이득임.

### 사용자 관점 성공률 (시험용, 등급 정답지 기준)

정답 1편만 세는 Recall 과 달리, **상위 10편에 쓸모 있는 논문(등급 2 이상)이 한 편이라도
있었는가**를 잼. 실제 사용자에게 중요한 것은 이쪽임.

| 구성 | easy | medium | hard | 전체 |
|---|---|---|---|---|
| 번역만 | 0.982 | 0.947 | 0.868 | 0.933 (평균 3.32편) |
| 검색어 2개 합치기 | 0.982 | 0.965 | **0.886** | **0.944** (평균 3.36편) |

**hard 층의 Recall 이 0.263 뿐이어도 88.6% 의 질문에는 쓸모 있는 논문을 한 편 이상 보여줌.**
hard 층 질문은 너무 두루뭉술해서 "질문을 만든 그 논문 1편"을 맞히는 것이 사람에게도 어려움.
hard 층을 평가할 때는 두 숫자를 반드시 함께 볼 것.

### 지금 서비스 구성의 층별 성능 (시험용 342문항)

| 구간 | Recall@10 | 후보 상한 | 회수율 | (참고) 번역만 쓰던 옛 구성 |
|---|---|---|---|---|
| 전체 | 0.617 | 0.713 | 0.865 | 0.608 |
| easy (대학원생, 정확한 학술어) | 0.930 | 0.982 | 0.947 | 0.904 |
| medium (학부 연구생) | 0.658 | 0.763 | 0.862 | 0.658 |
| **hard (1~2학년, 일상어)** | **0.263** | **0.395** | 0.666 | 0.263 |
| 한국어 | 0.591 | 0.708 | 0.835 | 0.567 |
| 영어 | 0.643 | 0.719 | 0.894 | 0.649 |

**hard 층은 시험용에서 전혀 좋아지지 않았음.** 개발용에서는 0.164 에서 0.198 로 올랐는데
시험용에서는 0.263 으로 같음. 가상 초록으로 얻은 이득은 easy 층에서 나왔음
(0.904 -> 0.930). **hard 층 문제는 아직 풀리지 않았다고 보는 것이 맞음.**

난이도가 설계 의도대로 단조롭게 나옴(easy > medium > hard). 옛 평가셋은 이것이 뒤집혀 있어서
폐기했음(ISSUE #32). 새 평가셋의 숫자는 믿고 쓸 수 있음.

### 확정된 것

| 내용 | 근거 |
|---|---|
| 로컬 의미 색인 71만 편이 성능의 대부분을 만듦 | 옛 평가셋에서 +0.243, p<0.0001 |
| 교차 인코더 재정렬이 효과 있음 | 옛 평가셋에서 0.343 → 0.427, p<0.001 |
| 한국어를 영어로 옮겨 넣으면 좋아짐 | 시험용 한국어 Recall +0.111(p=0.001), 영어 대조군 +0.000 |
| 난이도 축이 제대로 작동함 | easy 0.904 > medium 0.658 > hard 0.263 |

### 반증된 것 (졸업작품 보고서에 그대로 실을 것)

| 내용 | 근거 |
|---|---|
| 계층 변환(의도→개념→용어)이 단계마다 검색을 좋게 함 | 세 번 측정해 세 번 반증 |

### 판정 불가로 남아 있는 것 (2026-08-28 정정)

**아래 두 줄은 2026-08-28 이전에 '반증됨' 으로 적혀 있었음. 근거로 쓰던 nDCG 값이
성능이 아니라 판정 안 받은 논문을 센 값이어서 정정함.** 4절에는 2026-08-17 부터
"근거를 보류로 내렸음" 이라고 적혀 있었는데 이 표만 안 고쳐져 있었음.

| 내용 | 지금까지 잰 것 |
|---|---|
| 학습한 변환기(dpo)가 지금 구조에서 기여함 | Recall +0.018 (p=0.101, 판정 불가) · nDCG 는 못 읽음 |
| 두 채널을 합치면 좋아짐 | Recall +0.018 (p=0.101, 판정 불가) · nDCG 는 못 읽음 |

**왜 nDCG 를 못 읽는가 (2026-08-28 직접 세어 확인).** `results/test_dpo.jsonl` 의 상위
10편 3,420칸 중 **865칸(25.3%)이 arXiv 채널만 데려온 논문이고, 그중 등급이 매겨진 것은
32편(3.7%)뿐임.** 나머지는 `ndcg_at_k_single` 이 0점으로 셈. 등급 없는 논문을 목록에서
빼고 다시 세면 부호가 뒤집힘.

| 비교 (시험용 342문항) | 등급 없음을 0점으로 셈 | 등급 없음을 목록에서 뺌 |
|---|---|---|
| 로컬 단독 → 로컬 + arXiv | −0.043 (p<0.001) | **+0.020 (p<0.001)** |
| 변환기 없음 → 변환기(dpo) | −0.016 (p=0.008) | **+0.019 (p<0.001)** |

**두 셈법 모두 치우쳐 있어 어느 쪽도 못 씀.** 이 두 줄을 판정하려면 등급 정답지를
넓혀야 하는데, `app.py` 는 이미 arXiv 결과를 추천 목록에 섞지 않고 "최신 논문" 칸으로
따로 보여 주므로 **지금 서비스에 해당하지 않는 구성을 재려고 돈을 쓰는 일임.**
arXiv 채널을 계속 부를지는 다른 근거로 정할 것 — 시험용 342문항에서 **arXiv 채널이
혼자 정답을 데려온 문항은 13개(3.8%)** 이고, 그 대가로 응답 시간 약 5초를 씀.

---

## 2. 지금 막고 있는 문제 (2026-08-16 직접 확인)

### 문제 0 — 후보를 더 주면 오히려 나빠짐 (2026-08-16 측정)

| 로컬 후보 깊이 | Recall@10 | nDCG@10 | 쓸모 있는 논문 1편 이상 |
|---|---|---|---|
| **100** | 0.566 | **0.514** | **93.4%** (평균 3.14편) |
| 150 | 0.566 | 0.496 | — |
| 200 | 0.569 | 0.487 | — |
| 300 | 0.583 | 0.478 | 89.9% (평균 2.75편) |

후보 상한은 깊이 100 에서 0.635, 300 에서 0.721 로 확실히 오르는데 **최종 성능은 따라오지
않고(+0.017, p=0.214) 만족도는 확실히 떨어짐(−0.036, p<0.001).** 후보가 늘면 재정렬기가
엉뚱한 논문을 상위로 올리는 일도 함께 늘기 때문임. **그래서 서비스 깊이를 100 으로 정함.**

arXiv 채널을 순위 합치기로 섞는 것도 같은 이유로 손해임 (개발용, 로컬 300 + arXiv 100):

| 로컬 대 arXiv 가중치 | Recall@10 | nDCG@10 |
|---|---|---|
| 안 섞음 | 0.583 | **0.478** |
| 1 대 1 | 0.578 | 0.437 (**−0.041, p<0.001**) |
| 2 대 1 | 0.583 | 0.438 (**−0.040, p<0.001**) |
| 3 대 1 | 0.589 | 0.444 (**−0.034, p<0.001**) |

**로컬 비중을 올릴수록 손해가 줄지만 손익분기점을 넘지 못함.** 추세의 종점이 "섞지 않음"
이므로, arXiv 결과는 추천 목록에 섞지 않고 "최신 논문" 칸으로 따로 보여주기로 함.

### 문제 1 — hard 층은 재정렬이 아니라 1차 검색에서 막힘

> **2026-08-27 갱신:** 아래 진단은 옛 색인 기준임. 미세조정 색인에서는 1차 검색 쪽이
> 상당히 풀렸고(hard 후보 상한 개발용 0.336 → 0.474 · 시험용 0.395 → 0.500),
> **지금 막고 있는 것은 재정렬 쪽임.** 4절 1순위 참고.

개발용 348문항에서 정답 논문이 71만 편 중 몇 등인지 끝까지 계산했음(깊이를 잘라 보지 않고
전 구간을 봄). 로컬 의미 검색 한 채널, 한국어는 번역문 사용.

| 깊이 | 10 | 100 | 300 | 1000 | 3000 | 10000 |
|---|---|---|---|---|---|---|
| 전체 | 0.466 | 0.635 | 0.721 | 0.782 | 0.845 | 0.894 |
| easy | 0.784 | 0.914 | 0.957 | 0.974 | 0.983 | 1.000 |
| medium | 0.491 | 0.707 | 0.802 | 0.853 | 0.931 | 0.948 |
| **hard** | 0.121 | **0.284** | 0.405 | 0.517 | 0.621 | 0.733 |

hard 층 116문항의 정답 등수 분포: 1~10등 14편 · 11~100등 19편 · 101~1000등 27편 ·
1001~10000등 25편 · **1만등 밖 31편**.

즉 hard 층 실패의 첫 번째 원인은 **후보에 아예 없는 것**임.

**두 번째 원인은 재정렬기임.** 정답이 1차 검색 상위 10등 안에 들어와도 hard 층에서는 절반
가까이 버림.

| 난이도 | 정답이 1차 1~10등일 때 지킨 비율 | 11~30등 | 31~100등 | 101~300등 |
|---|---|---|---|---|
| easy | 0.99 (90/91) | 0.90 | 1.00 | 0.60 |
| medium | 0.93 (55/59) | 0.69 | 0.50 | 0.55 |
| **hard** | **0.57 (8/14)** | 0.43 | 0.50 | 0.29 |

**hard 층은 1차 검색과 재정렬이 함께 무너짐.** 한쪽만 고쳐서는 안 됨.

#### 시도한 처방 — 가상 초록(HyDE)을 두 번째 검색어로 쓰기

일상어 질문을 학술 용어로 건너게 하려고, 질문에 답할 법한 영어 초록을 지어내 그것으로도
검색했음. 개발용 348문항 측정:

| 방식 | 전체 | easy | medium | hard |
|---|---|---|---|---|
| 번역만 (기준선) | 0.583 | 0.922 | 0.647 | 0.181 |
| 가상 초록으로 **대체** | 0.555 | 0.862 | 0.603 | **0.198** |
| 두 검색어를 **합침** | **0.586** | 0.905 | 0.655 | **0.198** |

대체하면 hard 만 좋아지고 나머지가 나빠짐. 합치면 양쪽을 다 얻음. **그러나 시험용에서는
hard 층 이득이 재현되지 않았음**(0.263 그대로). 이득은 easy 층에서 나왔음.

**왜 hard 층에 잘 안 듣는지도 확인했음.** 4B 모델은 모르는 분야에서 그럴듯하지만 엉뚱한
초록을 지어냄:

```
질문      조건이 많은 문제를 아주 적은 메모리로 대충 잘 푸는 방법이 있나
가상 초록  ... adaptive neural compression ... selective feature extraction ...
정답 논문  Approximability of all Boolean CSPs with linear sketches
```

필요한 것은 "조건이 많은 문제 = 제약 충족 문제", "적은 메모리로 대충 = 스케치 근사" 라는
분야 지식인데, 모델에 그 지식이 없으면 지어낼 수 없음.

#### 2026-08-28 다시 잼 — 이득이 hard 가 아니라 한국어와 medium 에 있음

**위의 2026-08-16 표는 옛 색인에서 잰 것이고, 시험용 비교는 조건이 두 개 달랐음**(아래 참고).
미세조정 색인에서 **가상 초록 하나만 빼고 나머지를 전부 같게 두고** 다시 쟀음
(`runs/dev_ft_v2_nohyde.jsonl` — `--use-channels local_dense` 로 재정렬만 다시 돌림,
색인·검색어·재정렬 모델·후보 깊이 100·재정렬 깊이 100 동일).

**후보 상한 (재정렬 전에 정답이 후보 100편 안에 있는가)**

| 무리 | 번역문만 | 가상 초록만 | 합친 뒤 | 초록만 찾아냄 | 번역만 찾아냄 |
|---|---|---|---|---|---|
| 전체 | 0.655 | 0.578 | **0.727** | 0.106 | 0.184 |
| easy | 0.888 | 0.767 | 0.931 | 0.052 | 0.172 |
| medium | 0.672 | 0.638 | 0.776 | 0.138 | 0.172 |
| hard | 0.405 | 0.328 | 0.474 | 0.129 | 0.207 |

두 검색어가 **서로 다른 논문을 데려옴** — 초록만 찾아낸 문항 10.6%, 번역만 찾아낸 문항 18.4%.
합치면 +0.072 (p<0.001).

**최종 Recall@10 (재정렬까지 거친 뒤)**

| 무리 | 번역문만 | + 가상 초록 | 차이 | p | 새로 맞힘 / 잃음 |
|---|---|---|---|---|---|
| 전체 | 0.580 | 0.618 | +0.037 | 0.060 | 29 / 16 |
| easy | 0.871 | 0.914 | +0.043 | 0.069 | 6 / 1 |
| **medium** | 0.578 | **0.672** | **+0.095** | **0.007** | 14 / 3 |
| **hard** | 0.293 | **0.267** | **−0.026** | 0.590 | 9 / 12 |
| **한국어** | 0.540 | **0.615** | **+0.075** | **0.007** | 18 / 5 |
| **영어** | 0.621 | 0.621 | **+0.000** | 1.000 | 11 / 11 |

- **이득이 한국어에만 있음.** 영어는 정확히 +0.000 이고 11문항을 새로 맞히면서 11문항을 잃음.
  **영어 질문에서는 논문 22편을 바꿔치기만 하고 얻는 것이 없음**
- **hard 는 오히려 내려감.** 가상 초록을 넣은 원래 목적이 일상어 질문을 돕는 것이었는데
  미세조정 색인에서는 달성되지 않음. 9문항을 새로 맞히고 12문항을 잃음
- **이득은 medium 에서 나옴** (+0.095, p=0.007). 후보 상한도 medium 이 가장 크게 오름

**만족도는 판정할 수 없음.** 이번에는 **기준선 쪽** 등급 보유율이 0.677 임
(`grades_dev.jsonl` 이 가상 초록을 쓰는 구성 기준으로 넓혀져 있어서, 번역문만 쓰는 구성이
올리는 논문은 판정을 받은 적이 없음). 판정 없는 논문을 0등급으로 놓으면 +0.141(p<0.001),
3등급으로 놓으면 −0.083(p<0.001) 으로 부호가 갈림.

**지금 서비스 색인에서는 후보 상한 이득이 확정되지 않음.**

| 색인 · 평가셋 | 번역문만 | 합친 뒤 | 차이 | p |
|---|---|---|---|---|
| **미세조정 색인 · 개발용** | 0.655 | 0.727 | **+0.072** | **0.000** |
| 지금 서비스 색인 · 개발용 | 0.638 | 0.664 | +0.026 | 0.118 |
| 지금 서비스 색인 · 시험용 | 0.705 | 0.713 | +0.009 | 0.647 |

**옛 시험용 비교는 조건이 두 개 달랐음(2026-08-28 확인).** 문서가 인용하던 두 파일을
직접 열어 보니 가상 초록 유무 말고 재정렬 깊이도 달랐음.

```
results/test_translate_rerankraw.jsonl   채널 [local_dense]              rerank_depth 300
results/test_multiquery_d100.jsonl       채널 [local_dense, local_hyde]   rerank_depth 100
```

**따라서 "+0.009 (p=0.574)" 는 가상 초록만의 효과가 아니라 깊이 효과가 섞인 값임.**
깨끗하게 다시 재려면 시험용을 탐색에 써야 하므로 재지 않음(#25).

**생성 비용 (2026-08-28 직접 잼, Ollama `qwen3:4b`, 질문 12개).**

| 단계 | 중앙값 | 평균 | 최대 |
|---|---|---|---|
| 가상 초록 만들기 | **2.58초** | 2.83초 | 5.37초 |
| 한국어를 영어로 옮기기 | 0.36초 | 1.22초 | 10.41초 |

색인을 한 번 더 찾는 시간(워밍업 후 약 0.2초)을 더하면 전체 응답 15.1초 중 **약 2.8초**임.

**다시 볼 항목 — 영어 질문에는 가상 초록을 안 만드는 것.** 지금 `app.py:355` 는 언어와
무관하게 항상 만듦. 한국어일 때만 만들면 영어 질문의 응답 시간이 2.8초 줄고 잃는 것은
측정상 없음. **다만 영어 문항 174개에서 11 대 11 로 갈린 한 번의 측정이라 표본이 크지 않음.**
재정렬기 작업이 끝난 뒤에 다시 볼 것.

### 문제 2 — 서비스와 평가가 어긋나 있던 것 (ISSUE #39, #40) — 2026-08-16 해결

옛 `app.py` 는 로컬 후보 300편으로 재정렬 자리 300칸을 다 채워 **arXiv 후보를 한 편도 넣지
못했고**, 순위 합치기(RRF)를 아예 부르지 않았음. 학습한 변환기(dpo)는 매 검색마다 호출되며
GPU 8.64GB 를 쓰면서 결과에는 기여가 0 이었음.

고친 내용은 3절 ④ 참고. 고치면서 얻은 것:

| | 전 | 후 |
|---|---|---|
| 워밍업 후 응답 시간 | 21.1초 | **15.1초** |
| GPU (arXiv 끔) | 8.64GB + 2.6GB | **2.6GB** |
| 평가와 서비스의 후보 깊이 | 300 대 100 | **둘 다 100** |

같은 종류의 어긋남을 이미 두 번 겪었음(#10, #13). 세 번째였음.

### ~~문제 3 — 응답 시간이 기준을 넘음~~ **2026-08-28 해결 (기준 30초 안에 들어옴)**

**2026-08-28 재측정 (질문 10개, arXiv 끔, 색인 `cs2021-ft` + 순위 합치기 3:1).**
`app.run_search` 를 그대로 태워 쟀음 — 서비스가 실제로 도는 경로임.

| 단계 | 중앙값 | 평균 | 최대 |
|---|---|---|---|
| **추천 이유 생성** | **11.39초** | 11.18초 | 14.82초 |
| 두 번째 검색어(가상 초록) 만들기 | 1.05초 | 1.05초 | 1.38초 |
| 재정렬 (순위 합치기 포함) | 0.39초 | 0.40초 | 0.45초 |
| 한국어를 영어로 옮기기 | 0.37초 | 0.35초 | 0.47초 |
| 로컬 의미 검색 | 0.21초 | 0.21초 | 0.24초 |
| **합계** | **13.35초** | 13.12초 | **16.96초** |

**기준 30초를 넘은 질문 0/10개.** 서비스 시작 시 부품 적재 17.2초는 한 번만 듦.

- **옛 값 21.1초는 질문 2개로 잰 것이었음.** 지금은 10개로 쟀고 13.35초임
- **응답 시간의 85% 가 추천 이유 생성임.** 남은 줄일 자리는 사실상 여기 하나뿐임
- **순위 합치기는 시간을 안 씀** — 재정렬 0.39초 안에 들어 있음(등수를 더하는 계산뿐)
- 미세조정 색인으로 바꾼 것도 검색 시간에 영향 없음(0.21초)

**옛 측정 (2026-08-16, 질문 2개, arXiv 켬)** — 비교용으로만 남겨 둠.

| 단계 | 첫 질문 | 두 번째부터 |
|---|---|---|
| 논문 지목 확인 | 4.1초 | 0.4초 |
| 쿼리 변환 | 20.0초 | 3.5초 |
| 로컬 의미 검색 | 14.6초 | 0.2초 |
| arXiv 검색 | 1.5초 | 1.2초 |
| 재정렬 | 6.8초 | 1.3초 |
| 추천 이유 생성 | 26.1초 | 14.6초 |
| **합계** | **73.0초** | **21.1초** |

> **`evaluation/pipeline_eval.py --bench-service` 로 재면 안 됨.** 그 함수는 파이프라인을
> 자기가 다시 조립해서 **가상 초록도 순위 합치기도 안 씀.** 서비스와 다른 것을 재게 됨
> (#10 · #13 · #39 · #40 과 같은 자리). 위 값은 `app.run_search` 를 직접 부른 것임.

---

## 3. 모듈별 상태

⬜ 대기 · 🟨 진행 중 · ✅ 완료 · ❌ 폐기

| 모듈 | 코드 | 상태 | 지금까지 | 다음 |
|---|---|---|---|---|
| ① 쿼리 변환 | `src/rewriter/` (4개) | 🟨 | 번역 변환기가 유일하게 확정된 이득. 학습한 변환기(dpo)는 기여 판정 불가 | 서비스에서 dpo 를 뺄지 결정 |
| ② 검색 | `src/retrieval/` (4개) | 🟨 | 로컬 색인 71만 편 + 교차 인코더 재정렬 | 후보 깊이 재검토, arXiv 채널 처리 결정 |
| ③ 성능 평가 | `evaluation/` (4개) | ✅ | 평가셋 v2 + 시험용 기준선 확정 | 서비스 구성 확정 후 재측정 |
| ④ 웹 화면 | `app.py` | 🟨 | 화면 재구성, 번역 연결, arXiv 출처 표기, "못 찾았다" 기준선 0.002 | 후보 합치기 수정, 응답 시간 |
| 공통 | `src/`, `training/`, `tests/` | 🟨 | 코드 22개 파일, 단위 테스트 44개 통과. 산출물 이름을 모듈 기준으로 통일(#57) | 배포 준비(`pyproject.toml`, 라이선스, docker) |

### ① 쿼리 변환 — `src/rewriter/`

`base.py`(규약 + 생성 창구) · `baselines.py`(계층/한번에/HyDE/번역) · `finetuned.py`(학습 모델) ·
`paper_resolver.py`(특정 논문 지목)

- ✅ 언어 모델 Qwen3-4B 선정, 계층 변환기, 기준선 변환기 3종
- ✅ 검색 성공을 보상으로 삼은 학습(SFT → DPO). 옛 평가셋에서 0.167 → 0.343
- ✅ **번역 변환기** — 한국어 질문만 영어로 옮김. 학습 없이 프롬프트 한 줄. 시험용에서
  한국어 Recall +0.111(p=0.001), 영어 대조군 +0.000
- ❌ 정답 논문에서 라벨 뽑아 재학습(#17) · 어휘 조회(#22) · 용어 손질(#33) — 코드 삭제 완료
- ⬜ hard 층(일상어)을 도울 방법. 번역이 안 통하는 유일한 층임

### ② 검색 — `src/retrieval/`

`corpus.py`(적재 + 논문 번호 정규화) · `local_index.py`(71만 편 색인) · `arxiv_live.py` ·
`ranking.py`(RRF 융합 + 교차 인코더 재정렬)

- ✅ 코퍼스 716,183편(1.03GB) + 임베딩(2.93GB). 표본이 아니라 조건에 맞는 논문 전체임
- ✅ 메모리 15GB 에서 돌도록 만든 색인(본문은 파일 위치만 기록, FAISS 미사용, 조각 계산)
- ✅ 실시간 arXiv 검색 — 3초 간격, 지수 백오프, 소켓 시간 제한 90초(#38), 디스크 캐싱
- ✅ 논문 번호 정규화(#27), 색인·코퍼스 짝 확인(#28) — 둘 다 단위 테스트로 고정
- ✅ 교차 인코더 재정렬 `BAAI/bge-reranker-v2-m3`
- ⬜ 후보 깊이를 얼마로 할지 재결정 (지금 서비스 300 / 평가 100)

### ③ 성능 평가 — `evaluation/`

`dataset.py` · `pipeline_eval.py` · `metrics.py` · `report.py`

- ✅ 평가셋 v2 — 개발용 348문항(174짝) · 시험용 342문항(171짝). 언어와 난이도가 정확히 균등
- ✅ 등급 정답지 — 생성기 gpt-5.4, 심판 gpt-4.1-mini 로 계열을 갈랐음(#34)
- ✅ 채널별 결과를 따로 저장해 검색을 다시 하지 않고 재집계 가능
- ✅ 오류·결과 0건 문항을 제외하지 않고 실패로 셈(#23)
- ⬜ 실사용자 질문 수집(자동 생성 질문은 실제 검색어와 형태가 다름)

### ④ 웹 화면 — `app.py`

- ✅ 검색 전 입력창 하나, 결과는 목록 하나로 재구성(#11)
- ✅ 번역 결과를 화면에 보여 줌, 학습 모델에서 학술 용어를 뽑아 변환 과정 표시
- ✅ arXiv 출처 표기(CC0) + 후원·보증을 받지 않았다는 문구
- ✅ "못 찾았다"고 말할 기준선 0.002 — 등급 정답지 7,112쌍으로 실측해 정함
- ✅ **후보 구성을 새로 짬 (2026-08-16, #39 해결)**
      ⓐ 검색어 2개(번역 + 가상 초록)로 한 색인을 두 번 찾아 순위 합치기(RRF)로 합침
      ⓑ 후보 깊이 300 -> 100 (깊을수록 만족도가 떨어진다는 실측에 따름)
      ⓒ arXiv 결과는 추천 목록에 섞지 않고 "최신 논문" 칸으로 따로 보여줌
      ⓓ 학습한 변환기(dpo)는 arXiv 를 켤 때만 호출 (로컬 검색에는 쓸 곳이 없음)
      ⓔ 추천 이유 생성을 Ollama 로 돌림 (dpo 를 안 올릴 때 VRAM 8.64GB 를 아낌)
      -> 워밍업 후 응답 시간 21.1초에서 **15.1초**로 줄었음
- ✅ **2026-08-28 갱신** — 색인을 `cs2021-ft`(미세조정)로 바꾸고 순위 합치기 3:1 을 켬
      (`LOCAL_INDEX`, `FUSE_RERANK_WEIGHT`, `fuse_with_search`). 질문을 임베딩할 모델은
      색인 파일에 적힌 것을 읽어 쓰도록 해 어긋남을 막았음(#50). `MIN_RERANK_SCORE` 는
      다시 재고 0.002 유지 — 재정렬 모델을 안 바꿔 점수 눈금이 그대로임
- ⬜ 응답 시간 더 줄이기(추천 이유 생성 13.4초), 피드백 적재, docker 배포

---

## 4. 다음 우선순위

### 평가 기준을 고정했음 (2026-08-28)

**남은 기간 동안 지표를 바꾸지 않음.** 지금까지 구성마다 보던 지표가 달랐고(어떤 때는
Recall, 어떤 때는 nDCG, 어떤 때는 만족 편수) 등급 정답지를 그때그때 넓혀 절대값이
움직였음. 그러면 "좋아졌다" 가 지표를 고른 결과인지 모델이 좋아진 결과인지 못 가림.

| | 고정된 값 |
|---|---|
| 주 지표 | 개발용 348문항 **Recall@10** 하나. 등급 정답지가 필요 없어 돈이 안 들고 절대값이 안 움직임 |
| 기준선 | `runs/dev_service_repro.jsonl` (전체 0.575 · hard 0.181). 이 파일을 안 바꿈 |
| 채택 기준 | `paired_bootstrap` p < 0.05. 그 이상이면 "모름" 이고 채택 안 함 |
| 함께 보는 층 | hard(0.181) · 한국어(0.540) · 영어(0.621) |
| 통과 관문 | `evaluation/gates.py` 두 개. **떨어지면 Recall 을 아예 안 봄** |
| 만족도 | 판정 없는 논문을 0등급·3등급으로 각각 놓고 부호가 같을 때만 씀. 돈 안 씀 |
| 시험용 | 남은 기간 동안 **딱 한 번.** 관문을 통과하고 p<0.05 인 구성 하나만 묶어서 |

전문은 [../evaluation/README.md](../evaluation/README.md) 의 '고정된 평가 규칙' 절.


2026-08-17 갱신. **1차 검색 쪽이 재정렬 쪽보다 3배 큼** (시험용 342문항 기준).

| 어디서 실패하는가 | 문항 수 |
|---|---|
| **후보 100편 안에 정답이 아예 없음** | **98문항** (easy 2 · medium 27 · hard 69) |
| 후보에는 있는데 재정렬이 상위 10에서 버림 | 33문항 (easy 6 · medium 12 · hard 15) |

### 지켜야 할 측정 규칙 (2026-08-17 신설 → **2026-08-18 개발용에서 해제**, ISSUE #42)

**개발용에서는 nDCG@10 을 다시 쓸 수 있음.** 2026-08-18 에 판정 대상을 비교 구성 10개의
상위 10편 전체로 넓혔음($1.86, 판정 9,111건). 짝당 판정 편수가 20편에서 71편이 됨.

- 등급 보유율이 **1.000** 인 구성끼리는 그대로 비교하면 됨 —
  지금 서비스(재정렬 전/후), 원본질문 로컬, 번역문 로컬, 후보 깊이 200
- **변환기(dpo)와 arXiv 섞기는 아직 못 함**(보유율 0.807~0.823). `arxiv` 채널이 데려온
  로컬 색인 밖 논문 761편이 판정에서 빠지기 때문임
- **시험용은 그대로임.** `grades_test.jsonl` 은 안 넓혔으므로 옛 규칙이 살아 있음
- **확장 전후 nDCG 값을 섞어 비교하면 안 됨.** 판정 편수가 늘면 IDCG 가 커져 절대값이
  내려감. 확장 뒤에는 구성끼리의 차이만 볼 것
- **새 구성을 비교하려면 그 구성의 상위 10편을 넣어 한 번 더 판정해야 함**(약 $0.22~0.44)

### 1순위 — 2026-08-28 결론: 재정렬기 미세조정을 접고 순위 합치기를 채택함

**재정렬기 미세조정은 세 번 시도해 세 번 실패했음**(ISSUE #55). 마지막 시도는 학습 자료의
결함(오답 0편 64.5%)을 고쳐 다시 학습한 것인데, 점수가 뭉치는 고장은 풀렸으나 무관한
논문을 구별하는 능력을 잃고 Recall 이 0.618 에서 0.592 로 내려갔음.

**대신 재정렬기를 그대로 두고 재정렬 순위와 검색 순위를 합치는 쪽을 채택함.**

#### 개발용 348문항 최종 비교 (Recall@10, 고정 기준)

| 구성 | 전체 | easy | medium | hard | 한국어 | 영어 |
|---|---|---|---|---|---|---|
| 기준선 = 지금 서비스 | 0.575 | 0.914 | 0.629 | 0.181 | 0.575 | 0.575 |
| 미세조정 색인 + 원래 재정렬기 | 0.618 | 0.914 | 0.672 | 0.267 | 0.615 | 0.621 |
| **미세조정 색인 + 순위 합치기 3:1** | **0.635** | 0.914 | 0.681 | **0.310** | 0.632 | 0.638 |
| 미세조정 색인 + 새 재정렬기 | 0.592 | 0.914 | 0.612 | 0.250 | 0.580 | 0.603 |
| 미세조정 색인 + 새 재정렬기 + 순위 합치기 | 0.624 | 0.931 | 0.655 | 0.284 | 0.603 | 0.644 |

| 구성 | 기준선 대비 | p | 판정 | hard | p |
|---|---|---|---|---|---|
| 미세조정 색인만 | +0.043 | 0.031 | 채택 후보 | +0.086 | 0.017 |
| **미세조정 색인 + 순위 합치기** | **+0.060** | **0.003** | **채택 후보** | **+0.129** | **0.001** |
| 미세조정 색인 + 새 재정렬기 | +0.017 | 0.472 | 모름 | +0.069 | 0.108 |

**시험용에 올릴 구성: 미세조정 색인(`cs2021-ft`) + 원래 재정렬기 + 순위 합치기 3:1.**
관문 1·2 를 통과하고(재정렬기가 원래 것 그대로) 주 지표가 p=0.003 임.

#### 코드에 넣은 것 (2026-08-28)

- `app.py` 의 `FUSE_RERANK_WEIGHT = 3.0` · `fuse_with_search()`
- `evaluation/pipeline_eval.py` 의 `--fuse-rerank`
- 두 경로가 같은 순서를 내는지 확인하는 단위 테스트 2개
  (`tests/test_retrieval.py`, #10·#13·#39·#40 과 같은 자리)

**아직 안 한 것: `app.py` 의 색인을 `cs2021-ft` 로 바꾸는 것.** 순위 합치기는 지금 색인에서
효과가 없으므로(+0.006, p=0.583) 둘은 한 묶음임. **시험용 확정 판정 뒤에 함께 바꿀 것.**

### 옛 1순위 — 재정렬 단계 (2026-08-27 신설)

**왜 지금인가.** 2026-08-17 에 "재정렬 쪽에서 hard 층을 크게 끌어올릴 방법은 남아 있지
않음" 이라고 적었음. 그 판정은 **옛 1차 검색을 전제로 한 것**이었고, 전제가 바뀌었음.
미세조정 색인이 hard 층 후보 상한을 개발용 0.336 → 0.474, 시험용 0.395 → 0.500 으로
올렸는데 재정렬이 그것을 못 살리고 있음.

**저장된 값으로 다시 잰 상한 (개발용 348문항, 계산 비용 0원).** 재정렬 순위와 융합
순위를 어떻게든 완벽히 합쳤을 때 도달 가능한 Recall@10 임.

| 무리 | | 재정렬 상위10 | 융합 상위10 | 둘 중 하나라도 | 여유 |
|---|---|---|---|---|---|
| 전체 | 지금 색인 | 0.575 | 0.457 | 0.586 | +0.011 |
| 전체 | **미세조정 색인** | 0.618 | 0.540 | **0.664** | **+0.046** |
| hard | 지금 색인 | 0.181 | 0.129 | 0.207 | +0.026 |
| hard | **미세조정 색인** | 0.267 | **0.267** | **0.345** | **+0.078** |

**ISSUE #41 ⓓ 의 상한이 +0.026 에서 +0.078 로 세 배가 됐음.** 그리고 미세조정 색인의
hard 층에서는 **재정렬 순위와 융합 순위가 0.267 로 정확히 같음** — 재정렬이 융합보다
나은 것이 없는데, 서로 다른 문항을 맞히고 있어서 합치면 0.345 가 됨.

**다시 볼 항목이 하나 더 있음.** 2026-08-17 자르는 위치 실험(깊이 100/150/200)에서
"hard 층 후보 상한이 올라가면 다시 볼 항목으로 남김" 이라고 적어 뒀음. 그 조건이
충족됐음.

**2026-08-27 결정: 재정렬기 미세조정으로 바로 감.** 두 순위 합치기와 후보 깊이
재검토는 접었음. 후보 깊이는 **100 으로 고정.**

**왜 미세조정인가.** ISSUE #41 의 진단이 "hard 층에서 점수가 전부 무관 구간에 몰림" 이고,
그것은 순서 문제가 아니라 **점수 눈금 문제**임. 미세조정 색인에서 다시 재도 같음.

| 난이도 | 밀린 문항 | 정답 점수(중앙) | 10등 점수(중앙) | 못 알아봄(0.002 미만) |
|---|---|---|---|---|
| easy | 2 | 0.5432 | 0.5865 | 0.000 |
| medium | 12 | 0.0470 | 0.1758 | 0.083 |
| **hard** | **24** | **0.0035** | **0.0152** | **0.250** |

**상한:** 밀린 문항 38개를 전부 살리면 개발용 Recall@10 이 0.618 에서 **0.727**,
hard 가 0.267 에서 **0.474** 가 됨(후보 상한과 같은 값).

**학습 자료를 새로 만드는 중 (2026-08-27 15:06 시작, 약 4시간).** 기존
`train_retriever.jsonl` 은 못 씀 — 그 오답은 재정렬기가 이미 확실히 버리는 논문임
(점수 중앙 0.003 · 0.002 미만 41.3%). 새 자료는 `train_reranker.jsonl` 임.

**오답은 정답보다 점수가 높은 논문으로 고름.** 다만 실측해 보니 그 논문의 **77.1% 가
등급 2 이상, 즉 실제로 쓸모 있는 논문임.** 그래서 이분 라벨(관련/무관)을 쓰면 안 되고
**순서 손실**(`CachedMultipleNegativesRankingLoss`, 라벨 없음)을 씀. 표와 근거는
`training/build_retrieval_pairs.py` 설명글 참고.

**만들면서 잡은 결함 두 가지**

- 한국어 후보를 원문으로 뽑고 있었음. 서비스는 영어로 옮겨 검색함(1차 검색 100등 안
  영어 0.306 대 한국어 0.147). **같은 짝의 영어 질문으로 후보를 뽑도록 고쳤음**
  (`--pool-from-en`, 비용 0원). 재정렬기에 넣는 질문은 원문 그대로 둠
- 정답이 후보 100편 밖인 문항이 71.9% 임. 그래도 끼워 넣어 채점함 — 고치려는 것이
  순서가 아니라 점수 눈금이라 정답이 후보 밖이었는지는 상관이 없음. 다만 기록해 둠

**2026-08-27 에 한 것과 그 결과 (1차 시도, 실패)**

- 학습 쌍 33,000문항 + 검증 3,000문항 완성 (4시간 20분, `train/val_reranker.jsonl`)
- LoRA 미세조정 110.2분 → `models/reranker-ft`
- 개발용 348문항 판정 — **전체 판정 불가. 영어만 오르고 한국어는 내려감**

  | 무리 | 원래 재정렬기 | 미세조정 | 차이 | p |
  |---|---|---|---|---|
  | 전체 | 0.618 | 0.629 | +0.011 | 0.533 |
  | hard | 0.267 | 0.319 | +0.052 | 0.198 |
  | **한국어** | 0.615 | **0.586** | −0.029 | 0.232 |
  | **영어** | 0.621 | **0.672** | +0.052 | **0.033 유의미** |

- **원인은 점수 포화임.** 상위 10편에서 서로 다른 점수가 2,304개에서 **607개**로 줄고,
  동점이 31건에서 **432건**이 됐음. 1등과 10등의 차이가 hard 에서 0.1168 → **0.0249** 임.
  **점수가 0 에 몰리던 고장이 1 에 몰리는 고장으로 옮겨 갔음** — Gemma 를 폐기하게 만든
  것과 같은 실패 방향임(#55).
- **`MIN_RERANK_SCORE = 0.002` 가 죽음.** 상위 10편 중 0.002 미만이 0.000 임.

**다음 (2026-08-28 다시 씀 — 아래 '2026-08-28 정정' 참고)**

1. **학습 자료를 고침.** `build_retrieval_pairs.py` 의 오답 고르는 조건에서 `sc > gs`
   (정답보다 점수가 높은 것)를 뺌. 모든 문항이 오답 N편을 갖게 됨 (지금은 64.5%가 0편)
2. **손실은 `CachedMultipleNegativesRankingLoss` 그대로 두고** 다시 학습 (약 2시간).
   한 번에 하나만 바꿈(#23 점검표)
3. 개발용 판정. **점수 분포부터 확인할 것** — 상위 10편의 서로 다른 값 수와 1등·10등 차이.
   Recall 보다 이것을 먼저 봄
4. 3번을 통과하면 `app.py` 의 `MIN_RERANK_SCORE` 를 다시 잼
5. 통과하면 **미세조정 색인 + 미세조정 재정렬기를 한 묶음으로** 시험용에 한 번만 올림
6. 3번에서 점수 분포가 여전히 무너져 있으면 그때 손실을 `RankNetLoss` 로 바꿔 봄.
   자료는 1번에서 만든 것을 쓰되 열 구성을 `query` / `docs` / `labels` 로 바꿔야 함

**등급 정답지는 3번을 통과한 뒤에 셀 것.** 재정렬기가 바뀌면 상위 10편이 바뀌므로,
지금 필요한 쌍 수를 미리 계산해 두는 것은 의미가 없음.

**하지 않기로 한 것:** 대조군(`--negatives 0`) 학습. 어려운 오답을 아예 빼면 포화가 더
심해질 것이 뻔해서 이미 답을 아는 시험임.

### 2026-08-28 정정 — `RankNetLoss` 를 1순위로 두었던 근거가 틀렸음

위에 원래 적혀 있던 근거는 이것이었음.

> 손실을 `RankNetLoss` 로 바꿈. 두 논문의 점수 **차이**를 배우므로 절대값을 1 로 밀어
> 올릴 이유가 없음.

**설치된 라이브러리의 소스를 직접 읽어 확인한 결과, 지금 쓰는 손실도 똑같이 차이만
배움.** 두 손실 모두 점수 전체에 같은 상수를 더해도 손실값이 안 변함.

| 손실 | 계산 | 상수를 더하면 |
|---|---|---|
| `CachedMultipleNegativesRankingLoss` | `cross_entropy_loss(logits, labels=0)` — 소프트맥스 | 안 변함 |
| `RankNetLoss` (`LambdaLoss` 상속) | `sigmoid(sigma * (logits[:,:,None] - logits[:,None,:]))` | 안 변함 |

**따라서 손실을 바꾸는 것에는 점수가 1 쪽으로 몰리는 것을 막을 장치가 없음.**
바꿔야 할 것은 자료임. 자세한 것은 [ISSUE.md](ISSUE.md) #55 참고.

**해서는 안 되는 것:** 재정렬 모델 교체(#41 에서 반증), 재정렬 질문을 번역문으로 바꾸기
(효과 없음, 시험용을 이미 한 번 씀), 가상 초록 프롬프트 손질(#41 ⑤⑥⑦ 에서 반증),
후보 깊이 바꾸기(100 으로 고정).

### 옛 1순위 — 임베딩 모델 미세조정 (2026-08-18 신설, **2026-08-27 측정 완료**)

**왜:** hard 층 116문항 중 65문항은 두 검색어 어느 쪽으로도 못 찾음. 쿼리 변환 쪽으로
다섯 번 시험해 다섯 번 실패했음(6절 ⑦, 그리고 2026-08-18 의 두 가지). 남은 것이
검색 모델 자체를 학습시키는 것임.

**2026-08-18 에 한 것**

- 학습 데이터 생성 시작. `gpt-5-mini` + `reasoning_effort="minimal"`,
  코퍼스 분야 비율대로 논문 6,000편, 난이도 medium·hard 2종 → 질문 12,000개.
  예상 $5.52, 약 9.3시간. 평가셋 정답 논문 115편은 제외(#25)
  - `reasoning_effort` 를 안 주면 gpt-5 계열은 medium 이 걸려 호출당 추론 토큰이
    1,382개 붙고 값이 **6.9배**가 됨(실측). `dataset.py` 주석의 경고가 실제로 재현됨
  - easy 를 뺀 이유: 후보 상한이 0.931 로 올릴 자리가 없고, 개발용 easy 한국어 문항의
    **99.1%가 문장이 아니라 낱말 나열**임(문장으로 학습된 임베딩에 좋은 입력이 아님, 6절 ⑥)

- **오답 고르는 규칙을 실측으로 정했음(무료).** 대조 학습의 오답에 등급 2 이상 논문이
  섞이면 "좋은 논문을 내려라"를 가르치게 됨. 확장된 등급 정답지로 규칙 두 가지를 쟀음.

  | 오답 15편을 뽑는 규칙 | 전체 등급2+ | hard 등급2+ | 판정받은 비율 |
  |---|---|---|---|
  | 1~30등에서 (원래 안) | 0.426 | 0.478 | 0.920 |
  | 101~200등에서 | 0.422 | 0.487 | 0.099 |
  | **정답보다 유사도 0.05 낮은 것만** | **0.275** | **0.120** | 0.684 |
  | 정답보다 유사도 0.10 낮은 것만 | 0.210 | 0.000 | 0.419 |

  **등수를 깊게 하는 것은 안 통함** — 0.478 에서 0.487 로 그대로이고, 판정받은 비율만
  0.920 에서 0.099 로 떨어짐. "좋은 논문이 줄었다"가 아니라 "알 수 없어졌다"임.
  **유사도 간격은 통함** — hard 에서 4분의 1 로 줄고, 판정받은 비율이 0.684 로 높아
  값을 믿을 수 있음. 0.05 를 씀(오답 6편). 모자란 몫은 묶음 안 다른 질문의 논문으로 채움.
  이 규칙은 등급 정답지가 필요 없어 새 논문 6,000편에 그대로 쓸 수 있음.

**2026-08-19 에 한 것**

- 중단해 둔 생성을 09:20 에 이어서 시작해 12:02 에 다시 멈춤(2.7시간, 4,560/6,600회, $2.10)
- 그날 밤 23:10 에 마지막 2,040회를 이어서 돌려 **2026-08-20 00:23 에 생성을 끝냈음**
  (1.2시간, 호출 실패 0건, 입력 167.4만 · 출력 26.0만 토큰, $0.94)
- 누적 생성 비용 $2.49 + $2.10 + $0.94 = **$5.53** (처음 예상 $5.52 와 같음)
- **이어할 때 쓴 명령** — `--resume` 을 빼면 지금까지 만든 것이 지워짐(#48)

  ```
  $PY -m evaluation.dataset generate --n-papers 6000 --model gpt-5-mini \
      --effort minimal --sampling proportional --difficulties medium hard \
      --exclude-from data/eval/dev.jsonl data/eval/test.jsonl \
      --out data/training/train_queries.jsonl --seed 7 --resume
  ```

  `--seed 7` 이 같으면 뽑히는 논문 6,000편도 같음. 어제 만든 2,700편이 그 안에 전부
  들어 있음을 실행 전에 확인했음

**완성된 학습 데이터 (`data/training/train_queries.jsonl`, 14.4MB)**

| 항목 | 값 |
|---|---|
| 문항 | **24,000개** (논문 6,000편 × 난이도 2종 × 언어 2종) |
| 언어 | 한국어 12,000 · 영어 12,000 |
| 난이도 | medium 12,000 · hard 12,000 |
| 깨진 줄 · 빈 질문 · 짝 깨짐 | **0건** |
| 평가셋 정답 논문 115편과 겹침 | **0편** (#25 지켜짐) |
| 분야 | 110종, 코퍼스 비율 그대로 (cs.CV 17.0% · cs.LG 16.8% · cs.CL 10.1%) |

**학습 쌍을 만들 때 걸러야 할 것** — 생성 단계는 거르지 않고 기록만 함(`audit` 용).

- 초록을 5낱말 이상 그대로 옮긴 문항 **102개(0.4%)** — 평가셋 `split` 이 쓰는 기준과 같음
- 제목 겹침 0.5 이상 **276개** (중앙값은 0.0). 겹침은 그 자체로 잘못이 아니므로
  거르지 말고 층별로 나눠 성적을 보는 쪽이 맞음

**2026-08-25 에 한 것 — 1차 학습과 그 실패**

- 학습 쌍 만들기. 오답 고르는 규칙이 실제로는 오답을 하나도 못 만들던 것을 잡아
  재정렬기로 거르는 방식으로 바꿨음(#49)
- LoRA 학습 → 검색 모델 1차 학습본(**2026-08-28 에 지웠음**, [ARTIFACTS.md](ARTIFACTS.md) 2절).
  관문 1(부분집합 등수 확인)에서 **easy 층이
  0.836 에서 0.638 로 떨어졌음.** 학습 자료에서 easy 를 뺀 것이 원인이었음(#51)
- 색인을 만든 모델과 질문을 찾는 모델이 어긋나도 아무도 안 막던 자리를 막았음(#50)

**2026-08-26 에 한 것 — 자료를 고쳐 다시 학습**

- easy 질문 12,000개를 같은 논문 6,000편·같은 설정으로 만들어 넣음($2.77).
  문항 24,000개 → **36,000개**. 학습 설정은 하나도 안 바꿨음(#51)
- 학습 쌍 33,000문항(학습) + 3,000문항(검증) 재생성
- 학습이 그래픽카드 메모리에 두 번 안 들어갔고 `--grad-checkpoint` 로 해결(#52).
  131.3분, 최대 7,878MiB → `models/retriever-ft`

**2026-08-27 에 한 것 — 재색인과 판정**

- 관문 1 은 건너뛰었음(중간 값으로 방향을 바꾸지 않기 위함). 바로 전체 재색인
- 71만 편 재색인 **2시간 50분**, 70~98편/초. 검사 4종 통과 →
  `data/embeddings/cs2021-ft`
- 관문 2(개발용) 통과, 등급 정답지 1,233쌍 확장($0.262), 시험용 확정 판정
- **결과와 결정은 1절 참고. 서비스 색인 교체는 보류.**

**측정에서 배운 것 — 재색인 시간은 70편/초로 잡을 것.** 코퍼스 앞 4,096편으로 재면
98편/초가 나오는데 전체 평균은 70편/초임. 뒤로 갈수록 초록이 길어서임. 2026-08-11 에
원래 모델로 잰 73편/초가 맞는 값이었음.

**학습 데이터를 만들 때 평가셋 정답 논문 115편에서는 질문을 만들면 안 됨(#25).**
문서에 240편으로 적혀 있던 것은 틀렸음 — 실제로는 dev 58 + test 57 = 115편임.

### 종료된 항목 — 가상 초록의 용어 품질 (2026-08-18 종료)

**왜:** hard 층 1차 검색을 사실상 가상 초록이 지고 있음. 개발용 348문항에서 hard 층 정답을
가상 초록만 찾아낸 것이 18문항, 번역문만 찾아낸 것이 14문항임. 그런데 4B 모델이 분야
용어로 건너가지 못해 후보 상한이 0.319 에 머묾.

- ~~같은 프롬프트로 여러 번 뽑아 합치기~~ **2026-08-17 폐기.** 표본을 늘려도 같은 잘못된
  곳을 뒤짐(6절 상세 ④)
- **프롬프트로 용어를 끌어내기** — 분야, 문제 유형, 방법론을 먼저 적게 하고 그 용어로 초록을
  쓰게 함. JSON 필드 순서로 생성 순서를 강제함
- 그래도 안 되면 생성 모델을 키움(`qwen3:8b`). 다만 **배포 목표가 VRAM 8GB** 라 지금
  구성(약 5.2GB)에 8B(약 5GB)를 얹으면 넘침. 개발 장비에서 이득 크기만 먼저 확인함

### ~~2순위 — "못 찾았다" 기준선을 난이도별로 다시 잼~~ **2026-08-18 근거 없음으로 내림**

**옛 근거가 틀렸음.** "hard 층 정답 점수 중앙값이 0.0081 로 기준선의 4배뿐" 이라고 적혀
있었는데, 그 0.0081 은 ISSUE #41 표의 **'후보에는 있었는데 상위 10편에서 밀려난 정답
25편'의 점수**임. 밀려난 논문은 애초에 화면에 안 나오므로 기준선과 상관이 없음.

`runs/dev_service_repro.jsonl` 의 `rerank_scores` 로 직접 셌음(개발용 348문항).

| 난이도 | 상위10에 정답이 든 문항 | 그 정답 점수 중앙값 | 기준선 0.002 미만 |
|---|---|---|---|
| easy | 106편 | 0.9839 | **0편** |
| medium | 73편 | 0.7773 | **0편** |
| hard | 21편 | 0.0935 | **0편** |

**기준선 0.002 가 정답을 접은 일이 한 번도 없었음.** 상위 10편이 통째로 접혀
"딱 맞는 논문을 찾지 못했습니다" 가 뜬 문항도 0편임(hard 116문항 중 110문항은 10편이
그대로 보임). 상위 10편 중 접히는 논문은 hard 1.7%, easy 0.4% 뿐임.

**다만 기준선을 없애라는 뜻은 아님.** 이 기준선이 막으려던 것은 "코퍼스와 아무 상관 없는
질문에도 로컬 의미 검색이 항상 후보를 채워 돌려주는 것"인데(`app.py:88` 주석),
평가셋 348문항이 전부 코퍼스 안의 논문에서 만든 질문이라 **그 상황을 평가셋으로 잴 수
없음.** 지금 값이 해를 안 끼치는 것은 확인됐으니 그대로 둠.

### 2순위 — 서비스 마무리

응답 시간 재측정(질문 20개 이상), 추천 이유 생성 13.4초 줄이기, 좋아요/싫어요 적재,
`pyproject.toml`, 라이선스, docker 배포, 영문 README.

**배포 시 반드시 처리할 것 (arXiv 정책):** 요청 간격 3초는 `arxiv.Client` 인스턴스마다
지켜지는데, Streamlit 의 `@st.cache_resource` 는 프로세스마다 하나씩 만듦. 작업자를 2개
이상 띄우면 전체로는 규정을 넘김. 정책이 "기계를 늘려 제한을 넘지 말라" 고 명시적으로
금지한 상황임. 작업자 1개로 고정하거나 프로세스 밖 공유 잠금으로 간격을 관리해야 함.

### 나중에 — 코퍼스 주간 갱신 (우선순위 아님)

지금은 Kaggle 스냅샷 전체(약 4GB)를 다시 받아야 함. **OAI-PMH 부분 수확**(`from=` 으로
바뀐 것만)을 쓰면 주당 요청 3번이면 됨. 정책상 문제 없음(메타데이터는 CC0).
`build_embeddings` 의 이어하기 조건이 `count == n` 이라 코퍼스가 길어지면 71만 편을
3시간 동안 다시 계산함 - 뒤에 붙이기만 한 경우를 알아보도록 함께 고쳐야 함.

### 판단이 필요한 것 — arXiv 채널을 계속 부를 것인가

지금은 켜져 있고, 결과는 "최신 논문" 칸으로 따로 나감. **"어떤 비율로 섞어도 만족도가
떨어진다" 는 근거는 2026-08-17 에 보류로 내렸음**(ISSUE #42 — arXiv 채널에만 있는 후보는
1000편 중 1편만 판정을 받아 자동으로 0점이었음). 응답 시간을 약 5초 씀. 남은 근거는
**색인에 없는 최신 논문**뿐인데 그 가치는 이 평가셋으로 잴 수 없음. 총괄 판단이 필요함.

---

## 5. 알고 있어야 할 위험

| 위험 | 내용 |
|---|---|
| 시험용 평가셋이 마지막 깨끗한 자료임 | 시험용으로는 확정 판정만 하고 탐색은 개발용에서 함. 옛 평가셋은 이 규칙을 어겨 폐기했음(#25) |
| GPU 16GB, 시스템 메모리 15GB | 모델이 넘치면 오류가 아니라 조용히 CPU 로 밀려남. 추천 한 번이 10.5초에서 229.6초가 됨 |
| 배포 자원 | 코퍼스 1.03GB + 임베딩 2.93GB + 모델 3종. 지금 배포 설정은 3만 편 시절 기준이라 다시 짜야 함 |
| 평가로 잴 수 없는 것 | 평가셋 정답 논문이 전부 로컬 색인 안에 있어서, 색인 밖 논문(어제 올라온 논문)을 못 찾는 약점은 구조적으로 못 잼 |
| 자동 생성 질문 | 실사용자가 실제로 치는 검색어와 형태가 다를 수 있음 |

---

## 6. 날짜별 로그

| 날짜 | 한 일 | 결과 |
|---|---|---|
| 2026-08-28 | 재정렬기 학습 자료 고쳐 재생성(11분)·재학습(3시간 5분)·관문·개발용 판정, 순위 합치기를 코드에 넣음, 산출물 정리(약 17GB 삭제, 이름 규칙 통일, [ARTIFACTS.md](ARTIFACTS.md) 신설), **열려 있는 문제 4건을 저장된 파일로 다시 재서 검증** | **재정렬기 미세조정 폐기(#55, 세 번째 실패)**. 채택 후보 확정 — 미세조정 색인 + 순위 합치기 개발용 0.635 (+0.060, p=0.003 · hard +0.129, p=0.001). 이슈 #57 해결 |
| 2026-08-27 | 71만 편 재색인(2시간 50분), 관문 2, 등급 정답지 1,233쌍 확장, 시험용 확정 판정, 재정렬기 학습 쌍(4시간 20분)과 미세조정(1시간 50분) | 색인: 개발용 세 지표 유의미 · **시험용 전부 판정 불가**, 교체 보류. 재정렬기: **점수 포화로 1차 실패**. 이슈 #53·#54·#55·#56 신규 |
| 2026-08-26 | easy 질문 12,000개 추가 생성($2.77), 학습 쌍 재생성, 재학습(131.3분) | 문항 36,000개, `retriever-ft`, 이슈 #52 해결 |
| 2026-08-25 | 학습 쌍 만들기, 1차 학습, 관문 1 | **easy 층 0.836 → 0.638 로 잊어버림.** 이슈 #49·#50 해결 · #51 신규 |
| 2026-08-19 ~ 20 | 미세조정 학습 데이터 생성 마무리 | 문항 **24,000개** / 논문 6,000편 완성, 누적 $5.53, 이슈 #48 해결 |
| 2026-08-18 | 평가 하네스 재현성 수정, 1차 검색 실험 2종, 무료 심판 2종, **등급 정답지 확장**, 미세조정 학습 데이터 생성 | 재정렬 이득 확정(nDCG +0.078), 이슈 #42 개발용 해결 · #44~#47 신규 |
| 2026-08-17 | 재정렬 점수 저장, 자르는 위치 실험, 재정렬 진단, 가상 초록 3개 실험, 만족도 지표 편향 발견 | 재정렬 실패 원인 확정(#41), 이슈 #42·#43 신규 |
| 2026-08-16 | 문서 전면 정리, 후보 구성 실험 6종, 서비스 구조 재설계 | 시험용 Recall@10 **0.617** / nDCG **0.521**, 응답 시간 21.1초 -> 15.1초 |
| 2026-08-14 | 코드 정리(63→22개), 새 평가셋 기준선 확정, 화면 재구성, 번역 도입 | 시험용 0.608 / 0.512 |
| 2026-08-13 | 평가셋 v2 제작, 앱을 새 구조로 이전 | 질문 690개 + 등급 정답지 345짝 |
| 2026-08-12 | 두 채널 적용, 논문 번호 버그 수정 | 이후 공로 정정(#29) |
| 2026-08-11 | 로컬 71만 편 의미 검색 구축, 다채널 평가 하네스 | 개발용 0.637 |
| 2026-08-10 | 어휘 조회 편향 발견, 결론 철회 | 정정된 확정값 0.427 |
| 2026-08-09 | 교차 인코더 재정렬 도입 | 0.343 → 0.427 |
| 2026-08-07 | 평가 시스템 재구축, 시험용 300문항 전체 측정 | 0.167 → 0.343 |
| 2026-07-28 | 변환기 학습(SFT → DPO) | 40문항 0.150 → 0.350 |
| 2026-07-16 ~ 07-23 | 평가 설계, 코퍼스 3만 편, 변환 모듈, 웹 화면 | 서비스 경로 기준선 0.15 |
| 2026-03 ~ 07-06 | 저장소 생성, arXiv 검색 확인, 언어 모델 선정 | — |

### 2026-08-27 상세

**직접 실행해 확인한 것만 적음. 실행 결과 파일 이름을 함께 남김.**

#### ① 관문 1 을 건너뛴 이유

`training/train.py` 의 관문 1(부분집합 등수 확인)은 재색인 2시간을 아끼려고 만든 접는
판정임. 이번에는 **중간 값을 보고 방향을 바꾸지 않기 위해 건너뛰었음.** 판단 근거를
71만 편 전체에서 잰 값 하나로만 두기로 함. `embed-check` 는 부분집합을 스스로 다시
계산하므로 나중에 따로 돌릴 수 있음.

#### ② 재색인 (`data/embeddings/cs2021-ft`)

09:40 시작, 12:30 종료, **169.6분.** 속도가 앞부분 98편/초에서 뒷부분 70편/초로 계속
떨어졌음. 코퍼스 뒤로 갈수록 초록이 길어서임. 검사 4종을 통과했음 — 논문 수 716,183편,
기록된 모델 `models/retriever-ft`, 표본 9,000줄 전부 길이 1.000000(안 채워진 줄 0개),
코퍼스 짝 확인과 표본 확인 통과.

#### ③ 관문 2 — 개발용 348문항 (`runs/dev_ft_v2.jsonl`)

검색어는 `runs/dev_service_repro.jsonl` 에 저장된 것을 다시 씀(`--reuse-queries`).
가상 초록을 새로 만들지 않아 실행 간 흔들림이 0 임. 세 지표 모두 유의미하게 좋아졌음.
표는 1절 참고.

**순 차이 0 이 '안 바뀜' 을 뜻하지 않음.** easy 층은 Recall@10 이 0.914 로 같은데,
문항별로 보면 6문항을 새로 맞히고 6문항을 잃었음. 116문항 중 12문항의 답이 바뀌었음.
그리고 만족 편수 지표에서는 5.914 에서 5.603 으로 **유의미하게 나빠졌음(p=0.044).**

#### ④ 등급 정답지 확장 ($0.262)

미세조정 색인의 상위 10편 중 등급이 없는 1,233쌍을 `gpt-4.1-mini` 로 판정했음.
14분, 실패 0건. 보유율 0.623 → 1.000.

**확장 전후로 nDCG 차이의 부호가 뒤집혔음.**

| | 판정 전 (보유율 0.623) | 판정 후 (보유율 1.000) |
|---|---|---|
| nDCG@10 차이 | −0.108 (p<0.001) | **+0.022 (p=0.002)** |

−0.108 은 성능이 아니라 등급이 없는 논문 1,233쌍을 센 값이었음. ISSUE #42 와 같은
함정이고, 이번에는 $0.26 으로 확인했음. **새 구성을 만족도로 판정하기 전에 등급
보유율부터 셀 것.**

#### ⑤ 시험용 확정 판정 (`results/test_ft_v2.jsonl`)

**전부 판정 불가.** 개발용 +0.043(p=0.031)이 시험용에서 +0.012(p=0.632)가 됐음.
두 신뢰구간이 크게 겹치므로(개발용 [+0.006, +0.080], 시험용 [−0.032, +0.056])
서로 모순되는 결과가 아니라 **342문항으로는 이 크기를 잡아낼 힘이 모자란 것**임.

병목이 재정렬이라는 것이 두 평가셋에서 같은 방향으로 나왔음. 시험용 hard 층 편수:

```
후보에 정답이 든 문항   45편 -> 57편   (1차 검색이 12편 더 데려옴)
상위 10편에 올린 문항   30편 -> 32편   (재정렬이 2편만 더 올림)
```

#### ⑥ 재정렬 상한을 다시 쟀음 (비용 0원)

저장된 재정렬 점수와 채널 순위만으로 계산했음. ISSUE #41 ⓓ 의 상한이 옛 색인 +0.026
에서 미세조정 색인 **+0.078**(hard)로 세 배가 됐음. 표는 4절 1순위 참고.
**2026-08-17 의 "재정렬 쪽에 남은 방법 없음" 판정은 옛 1차 검색을 전제로 한 것이었고,
전제가 바뀌었음.**

### 2026-08-17 상세

**직접 실행해 확인한 것만 적음. 실행 결과 파일 이름을 함께 남김.**

#### ① 재정렬 점수를 저장하도록 고침 (`evaluation/pipeline_eval.py`)

`rerank_rows` 가 순서(`reranked_ids`)만 저장하고 모델이 매긴 점수를 버리고 있었음.
`rerank_scores` 를 함께 저장하게 함. 이것으로 **재정렬을 다시 돌리지 않고** 점수 분석과
깊이 비교를 할 수 있게 됐음 - 교차 인코더는 후보를 하나씩 독립적으로 채점하므로,
깊이 200 으로 한 번 채점해 두면 깊이 100 결과는 다시 줄 세우기만 하면 나옴.

`--channel-depth` 인자도 더함. 개발용 실행 결과가 채널당 300편으로 저장돼 있어 서비스
(채널당 100편)보다 유리한 조건이었음. **맞춰 재니 개발용 값이 0.586 이 아니라 0.580 이었음.**

진단 기능은 `--diagnose` 로 같은 파일에 넣었음(파일을 늘리지 않음, #36).

#### ② 자르는 위치 실험 — 깊이 100 유지 (개발용 348문항, `runs/dev_svc_d200.jsonl`)

서비스는 검색어 2개로 최대 200편을 가져온 뒤 융합에서 100편으로 자름(평균 174.3편 중
74편을 버림). 자르지 않으면 어떻게 되는지 잼.

| RERANK_DEPTH | 전체 | easy | medium | hard | 평균 후보 수 |
|---|---|---|---|---|---|
| **100 (지금)** | 0.580 | 0.897 | 0.638 | 0.207 | 100.0 |
| 150 | 0.598 | 0.914 | 0.655 | 0.224 | 149.6 |
| 200 (안 자름) | 0.598 | 0.922 | 0.647 | 0.224 | 174.3 |

깊이 150 전체 +0.017(p=0.068), 깊이 200 전체 +0.017(p=0.123). **전부 판정 불가.**
미리 정해 둔 규칙(p >= 0.05 면 응답 시간이 짧은 쪽 유지)에 따라 **100 을 유지함.**

다만 방향은 세 층 모두 일관되게 양수이고 p=0.068 은 경계에 가까움. "효과가 없다" 가
아니라 **"348문항으로는 +0.017 을 잡아낼 힘이 모자란다"** 가 맞는 서술임. hard 층 후보
상한이 올라가면 다시 볼 항목으로 남김.

#### ③ 재정렬 진단 — 실패 원인 확정 (ISSUE #41)

hard 층에서 정답 점수 중앙값이 **0.0081** 이었음. 순서가 뒤집힌 것이 아니라 모델이
관련성을 못 읽은 것임. 표와 해석은 [ISSUE.md](ISSUE.md) #41 참고.

동점(fp16 반올림)은 후보의 6.0% 에서 생겼고 hard 가 7.5% 로 가장 높았으나, 안정 정렬로
바꿔도 상위 10편 구성이 달라진 문항은 348개 중 2개뿐이었음. `ranking.py` 에
`kind="stable"` 을 적용했음 - 성능이 아니라 근거 없는 순서를 없애기 위함임.

#### ④ 가상 초록 3개 실험 — 폐기 (`runs/dev_hyde3.jsonl`)

같은 프롬프트, 같은 온도(0.7)로 가상 초록을 3개 만들어 채널 3개로 합침.

| 구성 | 전체 | easy | medium | hard |
|---|---|---|---|---|
| 번역문 + 초록 1개 (지금 서비스) | 0.675 | 0.922 | 0.733 | 0.371 |
| 번역문 + 초록 3개, 가중치 전부 1 | 0.652 | 0.888 | 0.707 | 0.362 |
| 번역문 1.0 : 초록 3개 각 0.333 | 0.678 | 0.922 | 0.733 | 0.379 |

가중치를 전부 1 로 두면 **나빠짐** - 채널이 3개가 되어 가상 초록 쪽 표가 3배가 되고
번역문 채널(단독 0.635 로 더 강함)이 밀려남. 가중치를 나눠 그 문제를 없애면 원위치임
(+0.003, hard +0.008). **생성 3회를 쓸 값어치가 없음. 폐기.**

**왜 안 되는지도 확인했음.** 세 표본은 서로 다른 논문을 데려옴(합집합 187.1편, 셋 다
겹친 비율 0.211). 그런데 **같은 잘못된 곳**을 뒤짐 - 셋 다 "novel approach for solving
complex optimization problems with minimal memory usage" 식으로 질문을 영어로 풀어 쓰기만 함.
**표본을 늘려도 모델이 모르는 용어는 나오지 않음.** 문제는 표본 수가 아니라 용어임.

#### ⑤ 가상 초록 프롬프트 3종 실험 — 전부 실패 (`runs/dev_hyde_prompts.jsonl`)

프롬프트 작성용으로 더 큰 모델에게 받아 온 세 가지를 시험함. 세 프롬프트 모두 "먼저
용어를 추론하고 그다음 초록을 써라" 형태라, JSON 필드 순서로 생성 순서를 강제했음
(앞 단계를 적을 자리가 없으면 지시가 무시되기 때문임).

융합 상위 100편(서비스와 같은 조건) 후보 상한임.

| 구성 | 전체 | easy | medium | hard |
|---|---|---|---|---|
| **번역문 + 지금 프롬프트 (지금 서비스)** | 0.675 | 0.922 | 0.733 | **0.371** |
| 번역문 + A(용어 추론) | 0.664 | 0.931 | 0.724 | 0.336 |
| 번역문 + B(용어 선택) | 0.675 | 0.940 | 0.733 | 0.353 |
| 번역문 + C(문제·대상·방법) | 0.647 | 0.914 | 0.707 | 0.319 |

**셋 다 hard 층에서 나빠졌음.** B 는 easy·medium 을 올렸으나 목표였던 hard 는 떨어짐.
B 만 생성 실패 8건이 났음(후보 용어 배열을 요구해 4B 모델이 형식을 놓침).

**왜 실패했는지 눈으로 확인했음.** 모델은 지시대로 용어를 제대로 떠올림.

```
질문        조건이 많은 문제를 아주 적은 메모리로 대충 잘 푸는 방법이 있나
B 후보 용어  [..., "constraint satisfaction problem", "combinatorial optimization", ...]
C 의 task   "Finding approximate solutions for complex constraint satisfaction problems"

B 초록      ... complex combinatorial optimization problems with extremely limited memory ...
C 초록      ... memory-efficient heuristic search algorithm for solving highly constrained ...
```

**떠올린 용어가 초록에 안 들어감.** "constraint satisfaction problem" 이라고 적어 놓고
초록은 더 흔한 말로 써 버림. 4B 모델이 매끄러운 문장을 만들려다 방금 고른 전문 용어를
흘리는 것임.

#### ⑥ 용어 자체를 검색어로 — 실패 (`runs/dev_terms.jsonl`)

⑤ 에서 "용어는 나오는데 초록이 안 쓴다" 를 봤으므로, 초록을 거치지 말고 뽑아낸 용어를
그대로 검색어로 넣어 봤음(생성 없이 저장된 `_terms` 필드로 시험).

| 구성 | 전체 | easy | medium | hard |
|---|---|---|---|---|
| 번역문 + 지금 초록 (지금 서비스) | 0.675 | 0.922 | 0.733 | **0.371** |
| B 의 용어 단독 | 0.497 | 0.802 | 0.457 | 0.233 |
| 번역문 + 지금 초록 + B 용어 | 0.670 | 0.948 | 0.707 | 0.353 |
| 번역문 + 지금 초록 + C 용어 | 0.664 | 0.931 | 0.707 | 0.353 |

깊이 100 에서 전부 나빠짐. 용어를 쉼표로 이어 붙인 글은 문장이 아니라, 문장으로 학습된
임베딩 모델에 좋은 입력이 아님.

#### ⑦ 1차 검색 쪽 세 번의 실패에서 얻은 것

하루 동안 쿼리 변환 쪽으로 세 가지를 시험해 **전부 실패했음.**

| 시도 | hard 후보 상한 (지금 0.371) |
|---|---|
| 같은 프롬프트로 초록 3개 뽑아 합치기 | 0.379 (가중치 고쳐야 겨우 본전) |
| 프롬프트 3종 | 0.319 ~ 0.353 |
| 뽑아낸 용어를 검색어로 | 0.353 |

**해석:** 지금 구조에서 쿼리 변환으로 짜낼 수 있는 것은 거의 다 짜냈음. 남은 것은
ⓐ 생성 모델을 키우기 ⓑ 임베딩 모델 자체를 학습시키기 둘뿐임. 프롬프트를 더 다듬는 데
시간을 쓰지 말 것.

#### ⑧ 재정렬 모델 교체 시험 — 반증 (`runs/dev_rr_gemma.jsonl`)

검색 모델(`bge-m3`)과 재정렬 모델(`bge-reranker-v2-m3`)이 **같은 바탕**임을 확인했음 -
둘 다 XLM-RoBERTa-large 를 BAAI 가 다르게 미세조정한 것이고 어휘 사전(250002)까지 같음.
바탕이 같으니 모르는 것도 같아서 두 단계가 같은 실수를 두 번 함. 그래서 바탕이 완전히
다른 `bge-reranker-v2-gemma`(GemmaForCausalLM, 2.51B, fp16 5.01GB)를 시험했음.

| 재정렬 모델 | 전체 | easy | medium | hard | hard 회수율 |
|---|---|---|---|---|---|
| 교차 인코더 (지금), 깊이 100 | **0.580** | **0.897** | 0.638 | 0.207 | **0.558** |
| Gemma, 깊이 100 | 0.575 | 0.853 | **0.664** | 0.207 | **0.558** |
| 교차 인코더, 깊이 200 | 0.598 | 0.922 | 0.647 | 0.224 | 0.510 |
| Gemma, 깊이 200 | 0.540 | 0.793 | 0.621 | 0.207 | 0.471 |

**미리 정해 둔 두 기준에 모두 불합격. 폐기.** hard 회수율이 0.558 에서 한 발짝도 안
움직였고, 깊이 200 에서는 전체가 유의미하게 나빠졌음(-0.057, p=0.004).

**원인은 점수 포화임.** Gemma 는 상위 10편의 **36.7%** 에 전부 최대값 18.7812 를 줌
(교차 인코더는 0.1%). 상위 10 안 동점이 1,178건으로 교차 인코더의 35건보다 34배 많음.
'관련 있음' 까지는 알지만 그 안에서 줄을 못 세움. 자세한 것은 [ISSUE.md](ISSUE.md) #41 참고.

#### ⑨ 만족도 지표의 판정 대상 편향 발견 (ISSUE #42)

등급 정답지가 "원본 질문으로 로컬 의미 검색을 해서 나온 상위 20편" 만 판정한 것이었음.
그 밖에서 논문을 끌어올리는 구성일수록 관련도와 무관하게 손해를 봄.
**2026-08-07 에 옛 #14 에 이미 적혀 있던 미착수 항목인데, 2026-08-16 문서 압축에서
잘려 나갔음**(ISSUE #43). 자세한 것은 [ISSUE.md](ISSUE.md) #42 참고.

**당장의 운영 규칙: 만족도(nDCG)로 구성을 고르지 않음. Recall@10 과 후보 상한으로만 판정함.**

---

### 2026-08-16 상세

**오전 — 문서 정리와 검증**

- 저장된 시험용 실행 결과 5개를 다시 집계해 문서의 숫자가 맞는지 확인함. 전부 일치함.
- `app.py` 의 후보 합치기를 실제로 실행해 확인함. 로컬 300편이 재정렬 후보 300칸을 다 채워
  arXiv 후보가 한 편도 들어가지 못함. 서비스는 순위 합치기(RRF)를 부르지 않음(#39).
- 개발용 348문항에서 정답 논문의 정확한 등수를 71만 편 전체에 대해 계산함. hard 층의 27%는
  정답이 1만등 밖에 있음.
- 실행 환경에 `pytest` 가 빠져 있어 설치함. 단위 테스트 39개 전부 통과함.
- 문서 네 개(PROGRESS · PLAN · MODULE_SPECIFICATION · ISSUE)를 지금 상태에 맞춰 다시 씀.

**오후 — 후보 구성 실험 6종 (전부 개발용)**

- **후보 깊이**: 100 / 150 / 200 / 300 을 같은 검색 결과 위에서 재정렬만 바꿔 비교함.
  깊을수록 상한은 오르지만 만족도가 떨어짐. **100 으로 정함.**
- **arXiv 합치기 비율**: 1대1 / 2대1 / 3대1. 어느 비율에서도 만족도가 확실히 떨어짐.
  **섞지 않고 따로 보여주기로 함.**
- **가상 초록(HyDE)**: 대체하면 hard 만 좋아지고 나머지가 나빠짐. **합치면 양쪽을 다 얻음.**
- 개발용에서 고른 구성(검색어 2개 합치기 + 깊이 100)을 **시험용에서 한 번만** 확정 측정함.
  Recall@10 0.617, nDCG@10 0.521. 옛 구성 대비 둘 다 +0.009 이고 **둘 다 판정 불가**임.
  hard 층 이득은 시험용에서 재현되지 않았음.
- `app.py` 를 새 구성으로 고치고 실제로 태워 확인함. 워밍업 후 응답 시간 15.1초.
- 재정렬기가 hard 층에서 정답을 43% 버린다는 것을 새로 발견함. 다음 과제로 남김.
