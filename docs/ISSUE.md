# 이슈 기록 (ISSUE)

개발 중 발생한 문제를 기록함. 상태 표기: 🔴 미해결 · 🟡 조사 중 · 🟢 해결 · ⚪ 폐기 판정

> **2026-08-16 정리:** 문서가 1,169줄까지 늘어 필요한 내용을 찾기 어려워져서 다시 정리했음.
> ① 지금 열려 있는 문제는 그대로 자세히 남김 ② 해결됐지만 **지금도 규칙으로 지켜야 하는 것**은
> 요약해 남김 ③ 나머지 해결·폐기 이슈는 한 줄 표로 압축함.
> 압축한 이슈의 원문 전체는 git 기록(커밋 `803b37a` 시점의 이 파일)에 그대로 남아 있음.
> 이슈 번호는 바꾸지 않았음 — 코드 주석과 다른 문서가 번호로 이 기록을 가리키기 때문임.

---

# 1. 열려 있는 문제

## #54 실행 정보 줄에 `--reuse-queries` 가 안 남아 다른 구성으로 기록됨 🔴 (2026-08-27 발견)

- **발생:** 2026-08-27, 미세조정 색인 결과를 기준선과 견주다가 표에 `passthrough` 로 찍혀서
- **문제점:** `pipeline_eval.py:1238` 이 만드는 `_meta` 줄에 `reuse_queries` 항목이 없음.
  `--reuse-queries` 로 저장된 서비스 검색어를 다시 써도 `_meta` 에는 `--rewriter` 의
  기본값인 `passthrough`(변환 안 함)가 적힘.

  ```
  runs/dev_ft_v2.jsonl 의 _meta   "rewriter": "passthrough"
  실제로 한 것                     저장된 서비스 검색어(번역문 + 가상 초록) 재사용
  ```

  `report.py` 가 그 이름으로 표를 찍고, 아래에 "변환하지 않은 passthrough 를 첫 번째로
  주는 것이 원칙" 이라는 안내문까지 붙임. **두 실행 모두 같은 서비스 검색어를 썼으므로
  그 안내문은 이 경우 맞지 않음.**

- **같은 자리를 이미 한 번 겪었음.** `results/test_multiquery_d100.jsonl` 의 `_meta` 가
  `rewriter: translate`, `channels: [local_dense]`, `rerank: none`, 커밋 667a657,
  2026-08-14 로 적혀 있는데, 파일 안에는 검색어 2개와 채널 2개와 재정렬 결과가 들어 있음.
  `--report-only` 에 `--out` 을 주면 결과 줄만 새로 쓰고 실행 정보 줄은 옛것을 그대로
  보존하기 때문임(`pipeline_eval.py:1176` 주석에 그렇게 하겠다고 적혀 있음).

- **왜 문제인가:** `results/README.md` 가 파일을 넣을 때 지킬 것으로 "어느 커밋인가 —
  실행 결과 파일의 `_meta` 줄에 자동으로 들어간다" 를 정해 뒀는데 그 값이 틀림.
  졸업작품 보고서가 인용하는 확정 결과를 그 커밋으로 되돌려도 재현되지 않음.

- **처방 (아직 안 함):** ⓐ `_meta` 에 `reuse_queries` 를 적기 ⓑ `--report-only --out`
  으로 쓸 때 옛 `_meta` 를 그대로 두지 말고 무엇을 다시 했는지 덧붙이기
  ⓒ `results/test_multiquery_d100.jsonl` 의 `_meta` 를 실제 실행에 맞게 고치기

## #53 미세조정 검색 모델이 1차 검색은 올렸으나 시험용에서 확정되지 않음 🔴 (2026-08-27)

- **발생:** 2026-08-27, 미세조정 색인 `cs2021-ft-v2` 의 시험용 확정 판정에서
- **한 일:** 임베딩 모델을 36,000문항으로 미세조정하고(`bge-m3-papers-v2`) 71만 편을
  다시 색인함(2시간 50분). 검색어는 저장된 것을 다시 써서 **색인만 바꿈.**

- **개발용 348문항 — 세 지표 모두 유의미하게 좋아졌음**

  | 지표 | 지금 색인 | 미세조정 색인 | 차이 | p |
  |---|---|---|---|---|
  | Recall@10 | 0.575 | 0.618 | +0.043 | 0.031 |
  | nDCG@10 | 0.662 | 0.684 | +0.022 | 0.002 |
  | 상위 10편 중 등급 2 이상 편수 | 6.057 | 6.287 | +0.230 | 0.015 |

- **시험용 342문항 — 전부 판정 불가**

  | 무리 | 지금 색인 | 미세조정 색인 | 차이 | p |
  |---|---|---|---|---|
  | 전체 | 0.617 | 0.629 | +0.012 | 0.632 |
  | easy | 0.930 | 0.886 | −0.044 | 0.155 |
  | medium | 0.658 | 0.719 | +0.061 | 0.187 |
  | hard | 0.263 | 0.281 | +0.018 | 0.734 |

  **"효과가 없다" 가 아니라 "있는지 없는지 모른다" 임.** 두 신뢰구간이 크게 겹침
  (개발용 [+0.006, +0.080], 시험용 [−0.032, +0.056]). 342문항으로는 이 크기를 잡아낼
  힘이 모자람.

- **원인이 특정됐음 — 1차 검색은 좋아졌는데 재정렬이 먹음.** 시험용 hard 층:

  | | 지금 색인 | 미세조정 색인 |
  |---|---|---|
  | 후보 상한 | 0.395 | **0.500** (+0.105, p=0.054) |
  | 회수율 | 0.667 | **0.561** |
  | 후보에 정답이 든 문항 | 45편 | 57편 |
  | 상위 10편에 올린 문항 | 30편 | 32편 |

  **1차 검색이 12편 더 데려왔는데 재정렬이 그중 10편을 버렸음.** ISSUE #41 이 병목임이
  개발용과 시험용 양쪽에서 같은 방향으로 나왔음.

- **결정: 서비스 색인 교체 보류.** 2026-08-16 에 검색어 2개 합치기를 채택할 때의 근거가
  "두 지표 어느 쪽도 나빠지지 않았음" 이었는데, 이번에는 easy 가 시험용 −0.044,
  개발용 만족 편수 −0.310(p=0.044)으로 나빠지는 방향이라 그 근거가 성립하지 않음.
  색인과 모델은 파일로 남겨 둠.

- **버릴 근거도 아님.** hard 층 후보 상한이 다섯 번의 쿼리 변환 시도로 한 번도 못 움직인
  값인데 올랐음. 재정렬 쪽이 풀리면 이 이득도 함께 드러남.

- **여기서 배울 것:** 미세조정처럼 **한 단계만 고치면 그 단계의 이득이 다음 단계의
  한계에 갇힘.** 1차 검색과 재정렬은 함께 봐야 함.

- **시험용을 한 번 썼음.** 다음 확정 판정까지 다시 보지 않음(#25).

## #52 학습이 그래픽카드 메모리에 안 들어가 두 번 멈췄음 🟢 (2026-08-26 해결)

- **발생:** 2026-08-26, easy 를 넣은 자료로 검색 모델을 다시 학습하려던 참
- **문제점:** 8월 25일과 **설정이 하나도 다르지 않은데** 1차 시도가 33초 만에 멈췄음.
  학습은 4,125걸음 중 0걸음이었음.

  ```
  torch.AcceleratorError: CUDA error: out of memory
  ```

  그때 카드가 16,376MiB 중 **16,032MiB** 까지 찼음. 시작할 때 여유가 14,523MiB 였음.
- **원인:** 다른 프로그램이 카드를 함께 쓰고 있었음. 학습 문항이 22,000개에서 33,000개로
  늘어난 것은 한 걸음당 메모리와 상관이 없음(걸음 수만 늘어남). 8월 25일에는 카드가
  비어 있었을 뿐이고, 이 학습은 원래 16GB 를 거의 다 쓰는 크기였음.
- **잘못 짚은 처방:** 메모리 조각남을 줄이는 `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`
  를 붙였더니 1초 만에 다른 오류가 남.

  ```
  RuntimeError: CUDA driver error: device not ready
  ```

  이때 쓴 메모리는 2,527MiB 뿐이라 **메모리 문제가 아님.** 이 방식은 메모리를 가상 주소로
  잡는 방법인데 이 실행 환경에서 그 방법이 제대로 안 돎. **메모리가 모자랄 때 붙이는
  물건이 아님.**
- **해결 — `--grad-checkpoint`.** 중간 계산값을 들고 있지 않고 필요할 때 다시 계산함.
  묶음 크기·오답 구성·손실 계산이 그대로라 **학습하는 내용이 안 바뀜**(#51 의 "바뀌는 것이
  자료 하나여야 한다"를 지킴). 느려지는 대신 메모리가 절반 아래로 내려감.

  | 시도 | 설정 | 결과 | 그래픽카드 메모리 최대 | 시간 |
  |---|---|---|---|---|
  | 1차 | 기본값 | 메모리 부족 | 16,032MiB | 33초 |
  | 2차 | 기본값 + `expandable_segments` | 장치 오류 | 2,527MiB | 1초 |
  | **3차** | 기본값 + `--grad-checkpoint` | **끝까지 학습** | **7,878MiB** | **131.3분** |

  예상 2시간 45분보다 빠른 2시간 11분에 끝났음. 다시 계산하는 값이 늘어도 묶음이 커져
  카드를 놀리지 않게 되는 쪽이 컸던 것으로 보임.
- **여기서 배울 것:**
  - **오래 도는 학습을 시작하기 전에 카드가 비었는지 확인할 것.**
    `nvidia-smi --query-gpu=memory.free --format=csv`
  - 묶음 크기를 줄이는 것은 이 학습에서 답이 아님. 대조 학습은 **묶음 안의 다른 문항이
    자동으로 오답이 되므로**, 묶음을 줄이면 메모리는 줄지만 학습 내용 자체가 달라짐.
    메모리만 줄이고 학습을 그대로 두는 방법이 `--grad-checkpoint` 임

## #51 학습 자료에서 easy 를 뺐더니 그 층을 잊어버렸음 🟢 (2026-08-25 발견, 2026-08-27 해결)

- **발생:** 2026-08-25, 검색 모델 미세조정 뒤 관문 1(부분집합 등수 확인)에서
- **문제점:** 일상어 층은 목표대로 크게 올랐는데 **정확한 학술어 층이 떨어졌음.**
  개발용 348문항, 부분집합 62,507편에서 100등 안에 정답이 든 비율임.

  | 무리 | 미세조정 전 | 미세조정 후 |
  |---|---|---|
  | **easy** | 0.836 | **0.638** |
  | medium | 0.655 | 0.888 |
  | **hard** | 0.250 | **0.776** |
  | 전체 | 0.580 | 0.767 |

  정답 등수 중앙값으로 보면 easy 는 2등에서 34등으로 밀렸고, hard 는 330등에서 20등으로 올랐음.

- **원인:** 학습 질문을 만들 때 **easy 를 일부러 뺐음**(2026-08-18 결정). 당시 근거는
  두 가지였음.

  ```
  ⓐ easy 는 후보 상한이 0.931 이라 올릴 자리가 없음
  ⓑ 개발용 easy 한국어 문항의 99.1%가 문장이 아니라 낱말 나열이라
     문장으로 학습된 임베딩에 좋은 입력이 아님
  ```

  둘 다 사실이지만 결론이 틀렸음. **올릴 자리가 없는 것과 잃을 자리가 없는 것은 다른
  이야기임.** 모델이 낱말 나열 형태의 질문을 한 번도 못 보고 문장형 질문만으로 학습해서,
  원래 하던 낱말 맞추기를 잊었음.

- **여기서 배울 것:** 어떤 층을 학습에서 빼면 그 층이 **그대로 있는 것이 아니라 나빠질 수
  있음.** 자료를 고를 때 "올릴 것이 있는가" 만 보지 말고 "지금 하고 있는 것을 잃지 않는가" 를
  함께 봐야 함.

- **처방 (2026-08-25 결정):** easy 질문을 **같은 논문 6,000편 · 같은 생성기 · 같은 설정으로**
  만들어 넣고 다시 학습함. 문항이 24,000개에서 36,000개가 됨. 비용 $2.77.
  학습 설정은 하나도 바꾸지 않음 - 바뀌는 것이 자료 하나여야 원인을 말할 수 있음.

  같은 씨앗(`--seed 7`)으로 다시 뽑은 6,000편이 이미 만든 것과 **정확히 같음을 돈 쓰기 전에
  확인했음**(겹침 6,000편, 빠진 것 0편, 평가셋 정답 논문과 겹침 0편).

- **쓰지 않기로 한 처방:** 원래 모델과 학습한 모델의 가중치를 배율로 섞어 두 층을 맞바꾸는
  방법을 검토했음(`training/train.py` 의 `blend`). 다시 학습할 필요가 없어 공짜지만,
  **배율이라는 값을 관측 결과에 맞춰 고르는 사후 조정**이라 시험용에서도 맞을 보장이 없음.
  도구는 남겨 두되 이번 처방으로는 쓰지 않음.

- **결과 (2026-08-27 확인):** 처방이 들었음. 개발용 348문항 Recall@10 에서 easy 가
  0.914 로 **떨어지지 않았음**(1차 학습 모델은 부분집합 100등 안에서 0.836 → 0.638
  이었음). medium 과 hard 는 올랐음.

- **다만 완전히 회복한 것은 아님.** easy 층을 세 지표로 나눠 보면 이렇게 갈림.

  | 지표 | 지금 색인 | 미세조정 색인 | p |
  |---|---|---|---|
  | Recall@10 | 0.914 | 0.914 | 1.000 |
  | nDCG@10 | 0.761 | 0.742 | 0.103 |
  | 상위 10편 중 등급 2 이상 편수 | 5.914 | **5.603** | **0.044** |

  **순 차이 0 이 '안 바뀜' 을 뜻하지 않음.** Recall@10 이 같아 보여도 문항별로는 6문항을
  새로 맞히고 6문항을 잃었음(116문항 중 12문항의 답이 바뀜). 만족 편수에서는 유의미하게
  나빠졌음.

- **`blend` 는 끝까지 쓰지 않았음.** 2026-08-27 에 다시 검토했으나 같은 이유로 안 씀.
  easy 손실은 문서에 한계로 적어 두는 쪽을 골랐음.

## #50 색인을 만든 모델과 질문을 찾는 모델이 어긋나도 아무도 안 막았음 🟢 (2026-08-25 해결)

- **발생:** 2026-08-25, 미세조정한 색인으로 평가를 돌릴 준비를 하다가 발견
- **문제점:** `evaluation/pipeline_eval.py` 의 `build_channels` 가 색인을 이렇게 만들었음.

  ```python
  local = LocalDenseRetriever(args.corpus, args.index, mmap=args.mmap)
  ```

  `--index` 는 인자로 받는데 **질문을 임베딩할 모델은 항상 `config.EMBED_MODEL`** 이었음.
  즉 미세조정한 색인을 주면서 질문은 옛 모델로 임베딩하게 됨. 두 벡터 공간이 달라서
  검색 결과가 무의미해지는데 **오류는 안 나고 성능만 조용히 무너짐.**
- **해결:** `--embed-model` 인자를 더해 색인을 만든 모델을 함께 받도록 함.
  `tests/test_retrieval.py` 에 단위 시험으로 못박았음.
- **같은 자리를 한 번 더 막았음:** `training/train.py` 의 `embed` 는 학습이 끝나면
  "색인을 **다른 이름으로** 새로 만들라" 는 명령을 찍어 줌. `build_embeddings` 는
  이어할 지점이 0 이면 `"w+"` 로 파일을 열어 기존 임베딩 2.93GB 를 덮어쓰기 때문임(#48 과 같은 종류).

## #49 오답 고르는 규칙(유사도 간격 0.05)이 실제로는 오답을 하나도 못 만들었음 🟢 (2026-08-25 해결)

- **발생:** 2026-08-25, 검색 모델 미세조정용 학습 쌍을 만들다가 발견
- **문제점:** 2026-08-18 에 실측으로 정한 규칙이 **"정답보다 유사도 0.05 낮은 것만 오답으로
  쓴다"** 였음. 그 규칙을 로컬 색인의 상위 200편에 적용하니 이렇게 됐음(문항 4,000개).

  | | 오답이 한 편도 안 나온 문항 |
  |---|---|
  | medium | 0.734 |
  | **hard** | **0.866** |
  | 전체 | 0.800 |
  | 정답이 상위 200 밖인 문항 | **1.000** |

- **원인:** 유사도가 아주 좁은 구간에 몰려 있음(문항 2,000개 실측).

  ```
  등수별 유사도 중앙값   1등 0.642 · 10등 0.610 · 100등 0.579 · 1000등 0.543
  정답의 유사도 중앙값   0.565      <- 정답이 대개 100~200등 언저리 점수임
  ```

  정답보다 0.05 낮으려면 0.515 아래여야 하는데 1000등이 0.543 임. 게다가 정답이 상위
  밖인 문항은 **상위 후보가 전부 정답보다 점수가 높아서** 규칙에 걸리는 것이 원리적으로 0 임.

- **왜 처음 측정에서는 통했는가:** 그 표는 **등급 정답지의 후보 묶음**(짝당 71편, 구성 10개의
  상위 10편을 모은 것)에서 잰 것임. 그 묶음은 여러 구성이 데려온 논문이 섞여 점수 폭이
  넓었음. 로컬 색인의 상위 목록은 성격이 다른 후보 묶음인데 임계값만 옮겨 왔음.
  **실측으로 정한 값이라도 재던 대상이 바뀌면 다시 재야 함.**

- **그대로 뒀다면:** 오답 자리가 무작위 논문으로 채워져 **정작 고치려던 일상어 층에서
  학습 신호가 통째로 비었을 것임.** 학습은 돌아가고 손실도 내려가는데 성능만 안 오르는,
  원인을 찾기 매우 어려운 실패가 됐을 것임.

- **해결 - 재정렬기로 거름.** 교차 인코더에게 상위 24편을 채점시켜 **점수가 가장 낮은 6편**을
  오답으로 씀. 넓힌 등급 정답지로 규칙별 오염도를 다시 쟀음(저장된 재정렬 점수를 써서
  새 계산 0회).

  | 오답 6편을 뽑는 규칙 | 전체 등급2+ | hard 등급2+ | 판정받은 비율 | 6편 못 채움 |
  |---|---|---|---|---|
  | 상위 30등에서 | 0.609 | 0.626 | 1.000 | 0.000 |
  | 재정렬 점수 0.002 미만 | 0.249 | 0.380 | 0.199 | 0.233 |
  | **재정렬 점수 낮은 것부터** | **0.160** | **0.172** | 0.090 | **0.000** |

  빈틈없이 잰 점수-등급 관계도 같은 방향임(판정 12,910건, 후보 100편 전부).

  | 재정렬 점수 | 등급0 | 등급1 | 등급2 | 등급3 | 등급2 이상 |
  |---|---|---|---|---|---|
  | 0 ~ 0.002 | 0.278 | 0.497 | 0.199 | 0.025 | **0.224** |
  | 0.002 ~ 0.02 | 0.143 | 0.509 | 0.296 | 0.051 | 0.347 |
  | 0.02 ~ 0.2 | 0.070 | 0.477 | 0.343 | 0.111 | 0.454 |
  | 0.2 ~ 1.01 | 0.021 | 0.308 | 0.416 | 0.254 | 0.670 |

  이 규칙이 정당한 근거가 하나 더 있음. 서비스는 재정렬 점수 0.002 아래 논문을 사용자에게
  접어서 안 보여 줌(`app.py` 의 `MIN_RERANK_SCORE`). **재정렬기가 무관하다고 한 논문은
  이 시스템이 이미 무관하다고 정의한 논문임.**

- **남은 한계:** 0.160 은 뽑힌 오답의 9.0% 만 등급을 받아서 잰 값이라 흔들림이 큼.
  빈틈없이 잰 것은 점수-등급 표(0.224)이고 그쪽도 같은 방향임.
- **결과:** 문항 24,000개 전부에서 오답 6편이 채워짐(못 채운 문항 0개).

## #48 학습 데이터 생성을 이어할 때 `--resume` 을 빠뜨리면 만든 것이 지워짐 🟢 (2026-08-19 해결)

- **발생:** 2026-08-19, 어제 45%까지 만들어 둔 미세조정 학습 데이터를 이어 만들려던 참
- **문제점:** 어제 밤 기록에 "같은 명령을 그대로 다시 치면 됨, `--resume` 이 기본" 이라고
  적혀 있었으나 **사실이 아님.**

  ```python
  # evaluation/dataset.py:1071
  g.add_argument("--resume", action="store_true", help="이미 만든 문항은 건너뛴다")
  ```

  `action="store_true"` 는 깃발을 안 주면 거짓임. 그러면 `cmd_generate` 가 `rows` 를
  빈 목록으로 시작하고, 10회째 호출마다 부르는 `write_jsonl` 이 `"w"` 모드로 파일을
  열어(`src/utils.py:28`) **어제 만든 10,800문항을 통째로 덮어씀.**
  돈으로 $2.49, 시간으로 3.9시간어치임.
- **처방:**
  - 이어할 때는 `--resume` 을 반드시 붙임. 명령 전문은 [PROGRESS.md](PROGRESS.md) 1순위 항목에 적어 둠
  - 오래 도는 생성을 시작하기 전에 결과 파일을 `.bak-날짜` 로 복사해 둠
    (`data/training/ft_queries.jsonl.bak-20260819`)
- **함께 확인한 것:** `--seed 7` 로 다시 뽑은 논문 6,000편 안에 어제 만든 2,700편이
  **전부** 들어 있음을 실행 전에 확인함. 표본이 흔들리지 않았음

## #47 `sample_papers` 가 분야를 잘못 나눠 뽑고 있었음 🟢 (2026-08-18 해결)

- **발생:** 2026-08-18, 미세조정 학습 데이터의 분야 분포를 정하려고 코드를 읽다 발견
- **문제점:** `evaluation/dataset.py` 의 층 나누기가 이랬음.

  ```python
  by_cat.setdefault(str(p["categories"]).split()[0] if p["categories"] else "?", []).append(p)
  ```

  `categories` 는 리스트임(`['math.NA', 'cs.NA']`). `str(리스트).split()[0]` 을 하면
  대괄호와 따옴표가 붙은 채로 잘림.

  ```
  ['math.NA', 'cs.NA']  ->  "['math.NA',"
  ['math.NA']           ->  "['math.NA']"     <- 같은 분야인데 다른 층
  ```

  분야가 하나뿐인 논문과 여럿인 논문이 갈려서 **같은 분야가 두 층이 됐음.** 층마다
  돌아가며 고르게 뽑으므로 두 층으로 쪼개진 분야는 두 배로 뽑혔음.

  | | 층 개수 |
  |---|---|
  | 고치기 전 | 197종 |
  | 고친 뒤 (`categories[0]`) | 156종 |

  두 층으로 쪼개진 분야: cs.CV(119,038편), cs.LG(111,949편), cs.CL(67,894편),
  cs.RO, cs.AI, cs.CR 등. 한 층뿐인 분야: math.NA, eess.SY 등.
  즉 **cs.CV 가 math.NA 의 두 배로 뽑히고 있었음.**

- **평가셋 v2 도 이 코드로 만들어졌음.** 다만 이미 확정 판정에 쓰이고 있으므로 다시
  만들지 않음(#25). 학습 데이터를 만들기 전에만 고쳤음.
- **해결:** `first_category()` 를 만들어 리스트의 첫 원소를 쓰게 함. 문자열로 들어오는
  경우도 함께 받음. `--sampling proportional` 을 더해 코퍼스 분야 비율 그대로 뽑는
  길도 만들었음(학습 데이터는 이쪽을 씀).

## #46 무료 심판으로 등급 정답지를 넓히려던 시도 ⚪ (2026-08-18 폐기)

- **왜 했는가:** #42 를 유료로 고치려면 돈이 드는데 잔고가 $0.43 이었음. 무료 모델
  (`qwen3:4b`)이 `gpt-4.1-mini` 를 대신할 수 있으면 0원에 넓힐 수 있었음.
- **미리 정한 기준:** 이차 가중 카파 0.6 이상 **그리고** 구성 순위 일치.
- **1차 — 한 번에 0~3 등급을 고르게 함. 카파 0.475 로 미달.**
  3,382번 판정해서 1등급을 6번, 2등급을 0번 줌. 0 아니면 3 이었음. 혼동표를 보면
  무관(92%)과 정답급(99%)은 맞히는데 그 사이에서 줄을 못 세웠음. #41 의 Gemma
  재정렬 모델이 상위 10편의 36.7%에 같은 최대값을 준 것과 같은 모양임.
- **2차 — 예/아니오 물음 세 개로 쪼갬. 카파 0.596 으로 미달(기준 0.6).**
  물음은 `JUDGE_SYSTEM` 의 등급 정의를 그대로 옮겼음.

  ```
  물음 1  조금이라도 관련 있는가        아니오 -> 0
  물음 2  읽으면 알고 싶던 것을 아는가   아니오 -> 1
  물음 3  주제를 정면으로 다루는가      예 -> 3 / 아니오 -> 2
  ```

  네 등급이 모두 나오게 됐고 카파가 0.475 에서 0.596 으로, 1등급 이내 일치가
  0.702 에서 0.849 로 올랐음. 구성 순위는 세 비교 모두 일치했음. **그러나 카파
  기준에 0.004 모자람.** 물음별로 채점하니 원인이 분명했음.

  | 물음 | 물어본 쌍 | 일치 | 과하게 예 | 과하게 아니오 |
  |---|---|---|---|---|
  | 1. 조금이라도 관련 | 3,382 | 0.785 | 1.1% | **20.4%** |
  | 2. 알고 싶던 것을 아는가 | 2,130 | 0.636 | **35.5%** | 0.8% |
  | 3. 정면으로 다루는가 | 2,018 | 0.389 | **61.0%** | 0.2% |

  물음 3 이 무너짐 — 물음 2 를 통과한 논문의 61%에 "정면으로 다룬다"고 답했음.
  4B 모델에게 '정면으로 다룸'과 '쓸모 있음'의 경계가 없음.
- **여기서 멈춘 이유:** 같은 3,382쌍을 보면서 물음 3 을 고치면 그 3,382쌍에 맞춘 것이
  되어 카파를 믿을 수 없음. 이어가려면 174짝을 절반으로 갈라 87짝에서 고치고 나머지
  87짝에서 한 번만 확인해야 함.
- **결말:** 미리 정한 규칙대로 폐기하고 유료로 넓혔음(#42 참고). 보고서에 실을 값어치는
  있음 — **관련도를 0/1 로 가르는 것과 3단계로 줄 세우는 것은 난이도가 다르고, 4B 급은
  뒤쪽을 못 함.** 재정렬(#41)과 심판에서 같은 크기대의 모델이 같은 자리에서 무너졌음.

## #45 평가 하네스가 서비스 구성을 만들지 못했음 🟢 (2026-08-18 해결)

- **발생:** 2026-08-18, 저장된 확정 결과를 다시 만들어 보려다 발견
- **문제점:** `results/test_multiquery_d100.jsonl`(보고서에 실은 Recall@10 **0.617**)은
  `local_hyde` 채널을 쓰는데, **`local_hyde` 라는 이름이 저장소 코드 어디에도 없었음.**
  `evaluation/pipeline_eval.py` 의 `build_channel()` 은 `arxiv` 와 `local_dense` 두 개만
  만들 수 있었고, 모르는 이름을 받으면 `ValueError` 를 냄.

  가상 초록으로 한 번 더 찾아 합치는 일은 `app.py` 의 `fuse_local()` 안에만 구현돼
  있었음. 즉 **서비스는 할 수 있는데 평가는 할 수 없는 구성**이었고, 확정 숫자를
  재현할 방법이 없었음. #10 · #13 · #39 와 같은 종류로 네 번째임.

- **왜 이 자리에서 자꾸 나는가:** 서비스와 평가가 같은 일을 각자 구현하기 때문임.
  `app.py` 는 화면을 그려야 해서 파이프라인을 자기 안에 두고, 평가는 배치로 돌려야 해서
  채널 구조를 씀. 두 구현이 갈라지는 것을 막는 장치가 없었음.

- **해결 (2026-08-18):**
  ⓐ `src/rewriter/baselines.py` 에 `ServiceRewriter` 추가 - 번역문과 가상 초록을 한 번에
     만듦. `app.py` 와 같게 **가상 초록은 원본 질문으로** 만들고, 번역기와 생성기가 같은
     Ollama 연결을 씀
  ⓑ `evaluation/pipeline_eval.py` 에 `local_hyde` 채널 추가. `build_channel` 을
     `build_channels` 로 바꿔 **로컬 색인을 한 벌만 올림**(임베딩 2.93GB, 시스템 메모리 15GB)
  ⓒ 가상 초록 생성이 실패하면 그 채널을 건너뛰도록 `LocalHydeChannel` 을 둠. 원본 질문으로
     대신 찾으면 로컬 색인이 같은 검색어로 두 번 표를 던져 서비스와 순위가 달라짐
  ⓓ **단위 시험 3개로 못박음** (`tests/test_retrieval.py`)
     - `app.fuse_local()` 과 `fused_ids_of()` 가 같은 순위를 내는가
     - 빈 검색어로 색인을 찾지 않는가
     - 채널이 둘이어도 색인을 한 벌만 올리는가

- **재현 확인 (개발용 348문항, `runs/dev_service_repro.jsonl`):**

  | | 문서에 적힌 값 | 다시 만든 값 | 차이 |
  |---|---|---|---|
  | Recall@10 | 0.580 | **0.575** | −0.005 |
  | 후보 상한 | 0.675 | **0.664** | −0.011 |
  | easy | 0.897 | 0.914 | +0.017 |
  | medium | 0.638 | 0.629 | −0.009 |
  | hard | 0.207 | 0.181 | −0.026 |

  가상 초록을 온도 0.7 로 생성하므로 실행마다 초록이 달라짐. **hard 층은 116문항이라
  실행 간 흔들림이 ±0.03 임을 알고 볼 것** - 이보다 작은 차이는 개선으로 읽으면 안 됨.

## #44 git 저장소의 객체 파일이 0바이트로 비어 손상됐음 🟢 (2026-08-18 해결)

- **발생:** 2026-08-18. `git log` 를 부르니 `fatal: loose object ... is corrupt` 가 남
- **문제점:** `.git/objects` 안에 **0바이트 파일이 10개** 있었고, 그중 하나가 그때의
  HEAD(`65ce26c`)였음. `git status` · `log` · `show` · `commit` 이 전부 실패함.
  `.git/logs/HEAD` 의 마지막 줄도 널 문자로 채워져 있었음.
- **원인:** 8/17 15:23 커밋 직후 쓰기가 끝나기 전에 WSL 이 끊긴 것으로 보임.
  **GitHub 로 밀어 넣는 것은 성공했고 로컬 파일만 비워졌음** - 그래서 "커밋은 됐는데
  그 뒤로 git 이 안 되는" 상태가 됨.
- **해결:** `.git` 을 통째로 백업한 뒤, 원격에서 새로 받아 성한 객체만 덮어씀.

  ```bash
  cp -a .git ~/git-backup-20260818
  git clone https://github.com/jmoongyu24/Papers-Please.git /tmp/pp-fresh
  find .git/objects -type f -empty -delete
  cp -rn /tmp/pp-fresh/.git/objects/* .git/objects/
  git fsck && git log --oneline -3
  ```

- **다시 겪지 않으려면:** 실험 결과와 문서를 고친 날은 그날 안에 밀어 둘 것.
  **원격에 있던 덕분에 하나도 잃지 않았음** - 로컬에만 있었으면 커밋 하나가 통째로 사라졌음.
- **작업 파일은 멀쩡했음.** 코퍼스 1.03GB, 임베딩 2.93GB, 결과 파일 전부 정상 크기였고
  단위 시험 39개가 그대로 통과했음. 손상은 `.git` 안에서만 났음.

## #42 등급 정답지가 '원본 질문 상위 20편' 만 판정해서 만족도 비교를 못 함 🟡 (2026-08-18 개발용 해결)

### 2026-08-18 해결 — 개발용 등급 정답지를 넓혔음 ($1.86)

**한 일.** `cmd_pool` 에 `--from-runs`(실행 결과에서 후보를 모음), `--from-runs-stage`
(재정렬 후만 / 융합 직후도 / 둘 다), `--from-runs-depth` 를 더했음. 비교할 구성
10개가 상위 10편에 올린 논문을 전부 판정 대상에 넣었음.

`--from-runs-stage both` 여야 하는 이유: `reranked_ids` 만 모으면 재정렬을 켠 구성의
상위 10편만 판정되고, 끈 구성이 올린 논문은 등급이 없어 0점이 됨. **#42 와 똑같은
잘못을 방향만 바꿔 저지르게 됨.**

| | 전 | 후 |
|---|---|---|
| 짝당 판정 편수 | 중앙 20편 (17~21) | **중앙 71편 (33~119)** |
| 전체 판정 | 3,556 | **12,667** |

새로 판정 9,111건, gpt-4.1-mini, 실제 비용 **$1.860**(쌍당 $0.0002042), 약 110분.
옛 판정 3,556건은 하나도 바뀌거나 사라지지 않았음(대조 확인). 복사본은
`data/eval/grades_dev.jsonl.bak-20260818`.

**결과 — 편향이 없어졌고 결론 하나가 뒤집혔음.**

같은 정답지를 두 방법으로 계산해 결론이 갈리는지 봤음. 방법 A = 등급 없는 논문을 0점
(지금 코드), 방법 B = 등급 없는 논문을 목록에서 뺌.

| 비교 (뒤 − 앞) | 확장 전 A | 확장 전 B | **확장 후 A** | **확장 후 B** |
|---|---|---|---|---|
| 재정렬 후 − 재정렬 전 | −0.008 (p=0.495) | +0.056 (p=0.000) | **+0.078 (p=0.000)** | **+0.078 (p=0.000)** |
| 번역문 로컬 − 원본질문 로컬 | +0.030 | +0.048 | **+0.038 (p=0.000)** | **+0.038 (p=0.000)** |
| 후보 깊이 200 − 깊이 100 | −0.020 | +0.014 | **+0.001 (p=0.902)** | **+0.001 (p=0.902)** |
| 변환기 dpo − 변환기 없음 | −0.020 | +0.028 | −0.025 | +0.022 |

앞의 세 비교는 등급 보유율이 **1.000** 이라 두 방법이 같은 값을 냄. **nDCG@10 을
그대로 쓸 수 있음.**

| 되돌린 판정 | 확장 전 근거 | **확장 후** |
|---|---|---|
| 교차 인코더 재정렬이 만족도를 해침 | nDCG −0.066 | **틀렸음. +0.078 (p<0.001) 로 이득임** |
| 후보 깊이 100 이 300보다 나음 | nDCG −0.036, p<0.001 | **차이 없음(p=0.902).** 응답 시간으로 100 유지 |

**아직 남은 것 (그래서 🟡).**

- **시험용(`grades_test.jsonl`)은 손대지 않았음.** 확정 보고값 Recall@10 0.617 /
  nDCG@10 0.521 은 옛 잣대의 값이고 그대로 유효함. 확장 전후 숫자를 섞어 비교하면 안 됨
  (판정 편수가 늘면 IDCG 가 커져 nDCG 절대값이 내려감).
- **변환기(dpo)와 arXiv 비교는 여전히 못 함.** 등급 보유율이 0.807~0.823 임.
  `arxiv` 채널이 데려온 논문 761편이 로컬 코퍼스(CS 계열 + 2021년 이후) 밖이라
  제목·초록을 못 꺼내 판정에서 빠짐. 66%가 2020년 이전 논문임. arXiv API 로 받아오면
  약 38분에 0원이지만, arXiv 채널의 값어치(최신성)는 이 평가셋으로 구조적으로 못 잼
  (정답 논문 115편이 전부 색인 안에 있음)이라 하지 않기로 했음.
- **새 구성을 비교하려면 다시 넓혀야 함.** 미세조정한 검색기가 올릴 새 논문은 등급이
  없음. 그 구성의 상위 10편을 넣어 한 번 더 판정해야 하고 약 $0.22~0.44 로 봄.

### (아래는 2026-08-17 최초 기록)


- **발생:** 2026-08-17. #41 을 재려다 만족도 숫자가 이상해 판정 대상을 확인하다 발견
- **새로 찾은 것이 아님. 2026-08-07 에 #14 에 이미 적혀 있었음.** 옛 #14 원문
  (`git show 803b37a:docs/ISSUE.md`) 에 이 문장이 있음.

  > 그런데 그 등급 평가는 **로컬 코퍼스 경로에서만** 했고, 지금 유일한 공식 기준인
  > arXiv 실시간 경로에서는 한 번도 하지 않았다. 근본 해결은 `judge_relevance.py` 를
  > **arXiv 실시간 결과에 적용**해 등급 정답지를 만들고 NDCG@10 을 함께 재는 것. **미착수.**

  2026-08-16 에 이슈 문서를 1,169줄에서 압축하면서 **#14 의 이 절반이 잘려 나갔음.**
  그 사이에 이 지표로 결정 두 건을 냈음(#39, #40).

- **문제점:** `evaluation/dataset.py` 의 `cmd_pool` 이 판정 대상을 이렇게 만듦.

  ```python
  for lang, r in sorted(langs.items()):
      for p in ret.search(r["text"], k=args.per_lang):   # 원본 질문, 로컬 의미 검색만
  ```

  기본값이 `--per-lang 15`, `--depth 20`. 즉 **판정 대상은 "원본 질문으로 로컬 의미 검색을
  해서 나온 상위 20편" 뿐임.** 실제 파일도 그러함 — `grades_dev.jsonl` 은 174짝 3,556편
  (짝당 중앙값 20편), `grades_test.jsonl` 은 171짝 3,486편.

  `metrics.py` 의 `ndcg_at_k_single` 은 등급이 없는 논문을 0점으로 셈. 그래서 그 20편
  밖에서 논문을 끌어올리는 구성일수록 **실제 관련도와 무관하게 손해를 봄.**

- **편향 크기 (실측):**

  | 구성 | 상위 10편 중 등급이 매겨져 있는 비율 |
  |---|---|
  | 1차 검색 결과 그대로 | 0.574 |
  | 재정렬 후 (지금 서비스) | 0.454 |
  | **arXiv 채널에만 있는 후보** | **0.001** |

  arXiv 가 찾아온 논문은 1000편 중 1편만 판정을 받았음. 나머지는 전부 0점으로 세어졌음.

- **영향 — 판정을 되돌려야 하는 결론 세 건:**

  | 결론 | 근거였던 값 | 상태 |
  |---|---|---|
  | 후보 깊이를 300 대신 100 으로 (#40) | nDCG −0.036, p<0.001 | **판정 불가로 내림** |
  | arXiv 를 섞지 않기로 (#39) | nDCG −0.041, p<0.001 | **판정 불가로 내림** |
  | 변환기(dpo)가 만족도를 해침 (#37) | nDCG −0.016, p=0.008 | **판정 불가로 내림** |

  세 건 모두 Recall 쪽 근거는 판정 불가였고(각각 p=0.214, p=0.537~1.000, p=0.101)
  nDCG 가 유일한 근거였음.

- **영향을 받지 않는 것:** 정답 논문은 항상 3등급으로 자동 부여되므로(`dataset.py` 의
  `grades[row["gold_id"]] = 3`) **Recall@10 과 후보 상한은 편향과 무관함.** 한국어를
  영어로 옮기는 이득(Recall +0.111, p=0.001), 로컬 색인의 기여, 재정렬의 Recall 기여는
  전부 유지됨.

- **"결론이 뒤집혔다"고 말하면 안 되는 이유:** 등급 없는 논문을 목록에서 빼고 재면
  순서가 반대로 나오지만(깊이 100 은 0.523->0.763, 깊이 300 은 0.489->0.782), 그 방식은
  **모르는 논문을 많이 올리는 구성에 유리한 반대쪽 편향**이 있음. 두 방식이 서로 반대를
  가리킨다는 것은 **"지금 정답지로는 답할 수 없음"** 을 뜻할 뿐임. 어느 값도 결론으로
  쓰면 안 됨.

- **해결 방법과 비용:** 비교할 구성들이 실제로 상위 10편에 올린 논문을 전부 판정 대상에
  넣어야 함(정보 검색의 표준 방식). 실측한 규모는 개발용 10,683쌍(약 $2.14),
  시험용 9,923쌍(약 $1.98)임.
  **2026-08-17 기준 OpenAI 잔고가 $0.43 이라 지금은 못 함.** 반쯤 채운 정답지는 지금 것보다
  나을 것이 없으므로 잔고를 쓰지 않고 남겨 둠.

- **그때까지의 운영 규칙:** **만족도(nDCG)로 구성을 고르지 않음. Recall@10 과 후보 상한으로만
  판정함.** 만족도는 참고로만 찍되, 찍을 때 반드시 등급 보유율을 함께 표시함.

- **무료 대안 (아직 안 해 봄):** Ollama 의 `qwen3:4b` 로 심판을 돌림. 쓰기 전에 이미
  유료로 판정된 3,556쌍을 다시 판정해 gpt-4.1-mini 결과와 얼마나 맞는지 검증해야 함
  (이 검증도 비용 0원). 일치도가 낮으면 폐기하고 예산 확보까지 미해결로 둠.
  #34 규칙은 지켜짐 — 질문 생성기가 gpt-5.4 라 다른 계열임.

## #43 문서를 압축할 때 미해결 항목의 본문이 잘려 나감 🟢 (2026-08-17 해결)

- **발생:** 2026-08-17, #42 를 조사하다 발견
- **문제점:** 2026-08-16 에 `ISSUE.md` 를 1,169줄에서 압축하면서, #14 의 "등급 평가가
  로컬 경로에서만 됐고 arXiv 경로는 미착수" 라는 절반이 사라졌음. 그 결과 **이미 알고
  있던 미해결 문제를 모르는 채로 그 지표를 근거로 결정 두 건을 냈음**(#39, #40).
- **해결:** ⓐ #14 에 잘린 내용을 되살리고 #42 로 확대함 ⓑ 아래 규칙을 #36 옆에 추가함.

  > **미해결(🔴 · 🟡) 이슈의 본문은 압축하지 않음.** 한 줄로 줄이는 것은 🟢 · ⚪ 만.
  > 미해결 이슈에 "미착수" 라고 적힌 항목은 그것이 곧 다음에 할 일이므로 특히 지우지 않음.

## #41 재정렬기가 hard 층에서 정답을 절반 가까이 버림 🔴

- **발생:** 2026-08-16, hard 층 실패를 1차 검색과 재정렬로 갈라 보다가 발견
- **문제점:** 정답이 1차 검색에서 이미 상위 10등 안에 들어와 있는데도 재정렬 뒤에 상위
  10편에서 사라짐. 난이도별로 차이가 큼 (개발용 348문항, 깊이 300).

  | 난이도 | 정답이 1차 1~10등일 때 지킨 비율 | 11~30등 | 31~100등 | 101~300등 |
  |---|---|---|---|---|
  | easy | 0.99 (90/91) | 0.90 | 1.00 | 0.60 |
  | medium | 0.93 (55/59) | 0.69 | 0.50 | 0.55 |
  | **hard** | **0.57 (8/14)** | 0.43 | 0.50 | 0.29 |

- **왜 중요한가:** hard 층은 이 프로젝트가 돕겠다고 한 사용자층임. 지금까지 hard 층 대책을
  전부 1차 검색 쪽에서 찾았는데(가상 초록 등), **후보가 있어도 재정렬에서 잃고 있었음.**
  1차 검색만 고쳐서는 이 손실이 그대로 남음.
- **짐작되는 원인(확인 안 됨):** 교차 인코더는 질문과 논문 초록의 어휘가 겹칠수록 높은 점수를
  줌. hard 층 질문은 일상어라 어느 논문과도 어휘가 안 겹쳐서, 정답과 오답의 점수가 다 낮게
  몰리고 순서가 뒤집히기 쉬움. 한국어 질문의 점수가 영어보다 낮게 나오는 것(만족 등급
  중앙값 0.07 대 0.17)도 같은 현상으로 보임.

### 2026-08-17 원인 확정 — '순서가 뒤집힌 것'이 아니라 '관련성을 못 읽은 것'

재정렬 점수를 저장하도록 고치고(`pipeline_eval.py` 의 `rerank_scores`) 처음으로 점수를
직접 봤음. 개발용 348문항, 채널 100편씩, 깊이 200.

정답이 후보에 있었는데 상위 10편에서 밀린 문항의 점수임.

| 난이도 | 밀린 문항 | 정답 점수(중앙) | 1등 점수(중앙) | 10등과의 차이(중앙) |
|---|---|---|---|---|
| easy | 3 | **0.9722** | 0.9946 | 0.0107 |
| medium | 14 | 0.1113 | 0.6531 | 0.0522 |
| **hard** | **25** | **0.0081** | **0.1387** | 0.0205 |

점수는 시그모이드 출력이라 "관련 있을 확률"에 해당함(모델이
`XLMRobertaForSequenceClassification`, 출력 라벨 1개 -> sentence-transformers 가
시그모이드를 씌움).

- **easy 의 실패는 모델 탓이 아님.** 정답이 0.97 을 받고 0.99 짜리에 밀린 것임. 모델은
  제대로 알아봤고 더 좋은 논문이 하나 더 있었을 뿐임. 이것은 #14(정답이 여러 편인데
  1편만 정답으로 셈)의 현상이지 재정렬의 결함이 아님. 3문항뿐임.
- **hard 의 정답 점수 중앙값은 0.0081 임.** 이 눈금에서 "무관" 에 해당함. 1등 오답도
  0.1387 로 낮아 상위 10편 전체가 무관 구간에 몰려 있음.
  **정답을 오답보다 낮게 매긴 것이 아니라 정답을 알아보지 못한 것임.**

**처방이 바뀜.** 점수가 아슬아슬하게 갈리는 것이었다면 1차 검색 순위를 섞어 보정할 수
있었음. 그런데 판정 자체가 틀렸으므로 순서를 손봐서는 못 고침. 모델을 바꿔야 한다고 봤음.
-> **같은 날 시험해 반증됐음(아래 '재정렬 모델 교체 시험' 참고). 바탕이 완전히 다른
모델로도 hard 회수율이 0.558 에서 안 움직였음.**

- **딸린 위험 — 서비스에 이미 걸려 있음.** `app.py` 의 `MIN_RERANK_SCORE = 0.002` 가
  "못 찾았습니다" 를 판정하는 기준선인데, **hard 층 정답 점수 중앙값 0.0081 이 그 기준선의
  4배뿐임.** 일상어 질문에서 정답을 제대로 찾아 올려도 '관련성이 낮아 접어 둔' 자리로
  내려갈 수 있음. 이 기준선은 2026-08-14 에 실측해 정했으나 **난이도별로 나눠 보지
  않았음.** 다시 재야 함.

- **동점도 확인했음 (효과는 거의 없음).** fp16 반올림으로 점수가 같아지는 자리가 후보
  60,644개 중 3,626개(6.0%)였고 hard 가 7.5% 로 가장 높았음. 그러나 안정 정렬로 바꿔도
  상위 10편의 구성이 달라진 문항은 348개 중 2개뿐이었음. `ranking.py` 의 `np.argsort` 에
  `kind="stable"` 을 적용했음 — 성능이 아니라 '판단 못 하는 자리에서 1차 검색 순서를
  따르게' 하려는 것임.

- **다음에 할 것:** ⓐ ~~정답 점수와 오답 점수의 분포를 갈라 봄~~ **완료(위)**
  ⓑ ~~재정렬 질문을 번역문으로 바꿔 봄~~ **완료, 효과 없음.** 2026-08-14 에 시험용에서
  이미 측정돼 있었음(`runs/test_translate_rerankEN.jsonl`): Recall +0.006(p=0.678),
  nDCG −0.007(p=0.226), hard 0.263 -> 0.254. 다시 재지 말 것 — 시험용을 이미 한 번 씀
  ⓒ ~~재정렬 모델 교체~~ **2026-08-17 반증됨. 아래 참고**
  ⓓ 1차 검색 순위와 함께 쓰기 — **상한이 낮음.** 저장된 순위로 미리 재 보니 두 순위를
  어떻게 합쳐도 Recall@10 상한이 0.606 이었음(지금 0.580). 최대 +0.026 임. ⓒ 가 반증된
  지금은 재정렬 쪽에 남은 유일한 수단이지만, 이 상한을 알고 시작할 것
  ⓔ 난이도별로 `MIN_RERANK_SCORE` 를 다시 잼 (`--calibrate-threshold`).
  **ⓒ 를 폐기했으므로 이것은 그대로 남아 있는 위험임 - 우선순위를 올려야 함**

- **2026-08-17 종합 판정:** 재정렬 쪽 처방 다섯 가지 중 넷이 반증됐음(ⓐ 진단 완료 ·
  ⓑ 번역문 질문 · ⓒ 모델 교체 · 동점 처리). 남은 것은 ⓓ(상한 +0.026)와 ⓔ(위험 관리)뿐임.
  **재정렬 쪽에서 hard 층을 크게 끌어올릴 방법은 지금 남아 있지 않음.**
  hard 층 처방은 1차 검색 쪽(생성 모델 키우기, 임베딩 미세조정)으로 옮겨야 함.

### 2026-08-27 갱신 — 옛 판정의 전제가 바뀌었음

위의 "재정렬 쪽에 남은 방법 없음" 은 **옛 1차 검색을 전제로 한 것**임. 미세조정 색인
(`cs2021-ft-v2`)이 hard 층 후보 상한을 개발용 0.336 → 0.474, 시험용 0.395 → 0.500 으로
올렸고, **재정렬이 그것을 못 살리는 것이 지금 병목임**(#53).

**ⓓ(1차 검색 순위와 함께 쓰기)의 상한을 저장된 값으로 다시 쟀음.** 계산 비용 0원,
개발용 348문항. 재정렬 순위와 융합 순위를 완벽히 합쳤을 때 도달 가능한 Recall@10 임.

| 무리 | | 재정렬 상위10 | 융합 상위10 | 둘 중 하나라도 | 여유 |
|---|---|---|---|---|---|
| 전체 | 지금 색인 | 0.575 | 0.457 | 0.586 | +0.011 |
| 전체 | 미세조정 색인 | 0.618 | 0.540 | **0.664** | **+0.046** |
| hard | 지금 색인 | 0.181 | 0.129 | 0.207 | +0.026 |
| hard | 미세조정 색인 | 0.267 | **0.267** | **0.345** | **+0.078** |

**ⓓ 의 상한이 +0.026 에서 +0.078 로 세 배가 됐음.** 그리고 미세조정 색인의 hard 층에서
**재정렬 순위와 융합 순위가 0.267 로 정확히 같음** — 재정렬이 융합보다 나은 것이 없는데
서로 다른 문항을 맞히고 있어서 합치면 0.345 가 됨.

**ⓔ(난이도별 기준선 다시 재기)는 2026-08-18 에 근거 없음으로 내려갔음** — 기준선 0.002 가
정답을 접은 일이 개발용 348문항에서 0건이었음. PROGRESS 4절 참고.

**다시 볼 항목:** 2026-08-17 자르는 위치 실험(깊이 100/150/200)에 "hard 층 후보 상한이
올라가면 다시 볼 항목으로 남김" 이라고 적어 뒀음. 그 조건이 충족됐음.

### 2026-08-17 재정렬 모델 교체 시험 — 반증됨 (`runs/dev_rr_gemma.jsonl`)

**가설:** 검색 모델(`bge-m3`)과 재정렬 모델(`bge-reranker-v2-m3`)이 **같은 바탕**임을
확인했음 - 둘 다 XLM-RoBERTa-large(24층 · 은닉 1024 · 어휘 250002)를 BAAI 가 다르게
미세조정한 것임. 바탕이 같으니 모르는 것도 같아서 두 단계가 같은 실수를 두 번 함.
그래서 **바탕이 다른** 모델이면 풀릴 것으로 봤음.

`BAAI/bge-reranker-v2-gemma` 를 골랐음. 유일하게 바탕이 완전히 다름(`GemmaForCausalLM`,
2.51B, fp16 5.01GB). 후보 중 `jina-reranker-v2-base-multilingual` 은 어휘 250002 까지
지금 것과 같은 XLM-R 계열이라 이 진단에는 맞지 않아 제외했음.

**결과 (개발용 348문항, 채널 100편씩):**

| 재정렬 모델 | 전체 | easy | medium | hard | hard 회수율 |
|---|---|---|---|---|---|
| 교차 인코더 (지금), 깊이 100 | **0.580** | **0.897** | 0.638 | 0.207 | **0.558** |
| Gemma, 깊이 100 | 0.575 | 0.853 | **0.664** | 0.207 | **0.558** |
| 교차 인코더, 깊이 200 | 0.598 | 0.922 | 0.647 | 0.224 | 0.510 |
| Gemma, 깊이 200 | 0.540 | 0.793 | 0.621 | 0.207 | 0.471 |

미리 정해 둔 두 기준에 모두 불합격임. hard 회수율이 0.558 에서 **한 발짝도 안 움직였고**,
깊이 200 에서는 전체가 유의미하게 나빠졌음(-0.057, p=0.004). **폐기.**

**왜 안 됐는지 - 점수가 포화됨:**

| | 상위 10편 점수 중 서로 다른 값 | 최대값에 붙은 비율 | 상위 10 안 동점 |
|---|---|---|---|
| 교차 인코더 | 2,300 / 3,480 | 0.1% | 35건 |
| Gemma | 1,387 / 3,480 | **36.7%** | **1,178건** |

Gemma 는 상위 10편의 36.7% 에 전부 최대값 18.7812 를 줌(세 난이도 모두 1등 점수
중앙값이 18.7812 로 똑같이 찍힘). **'관련 있음' 까지는 알지만 그 안에서 줄을 못 세움.**

교차 인코더의 실패(hard 에서 점수가 전부 0 에 몰림)와 Gemma 의 실패(상위권이 전부
최대값에 몰림)는 **반대 방향의 같은 문제**임. 둘 다 hard 층에서 순위를 만들 신호를 못 냄.

**확정된 것: "재정렬 모델을 바꾸면 hard 층이 풀린다" 는 가설은 반증됐음.**
바탕이 완전히 다른 모델로도 안 움직였으므로, 다른 재정렬 모델을 더 시험할 근거가 없음.

**구현 중 겪은 함정 두 가지 (되풀이하지 말 것).** 첫 실행에서 Recall 이 전부 0 이 나왔는데
모델 탓이 아니라 구현 오류였음. ⓐ Gemma 는 **왼쪽 채움(padding)** 인데 오른쪽 채움을
가정해 엉뚱한 자리의 로짓을 읽었음 ⓑ 다음 낱말을 예측하는 모델이라 **지시문이 맨 뒤**에
와야 하는데 맨 앞에 뒀음. 둘 다 오류 없이 조용히 순위만 무너짐(#28 과 같은 종류).
`ranking.py` 의 `LLMReranker` 주석에 남겼음.

**부수 실측 (8GB 배포 참고):** 실행 중 VRAM 이 15,990 MiB 까지 올라갔음. 모델 5.01GB 가
아니라 `logits` 텐서가 원인임 - 배치 8 x 512칸 x 어휘 256,000 x 2바이트 = 약 2.1GB.
마지막 한 칸만 필요하므로 줄일 수 있으나, 폐기했으므로 필요 없어졌음.

## #40 평가와 서비스의 후보 깊이가 서로 다름 🟢 (2026-08-16 해결)

- **발생:** 2026-08-16, 서비스 코드와 저장된 실행 결과를 대조하다 발견
- **문제점:** 로컬 의미 검색에서 가져오는 후보 수가 다름.

  | | 로컬 후보 | arXiv 후보 | 재정렬 후보 |
  |---|---|---|---|
  | 서비스 `app.py` | **300편** | 100편 | 300편 |
  | 평가 `results/*.jsonl` | **100편** | 100편 | 300편(실제로는 약 200편) |

- **왜 문제인가:** 후보 깊이는 성능에 직접 영향을 줌. 개발용 348문항에서 정답 논문의 정확한
  등수를 71만 편 전체에 대해 계산해 보니 후보 상한이 깊이 100 에서 0.635, 깊이 300 에서
  0.721 임.
- **재 보니 예상과 반대였음.** 상한이 오른 만큼 최종 성능이 오르지 않았고, 만족도는 확실히
  떨어졌음 (개발용 348문항, 같은 검색 결과 위에서 재정렬 깊이만 바꿔 비교).

  > **2026-08-17 보류.** 아래 nDCG 값은 #42(등급 정답지가 원본 질문 상위 20편만 판정)의
  > 영향을 받음. 깊이가 깊어질수록 판정 안 받은 논문이 위로 올라와 자동으로 손해를 봄.
  > Recall 근거는 원래 판정 불가였으므로(+0.017, p=0.214) **이 결정은 근거가 없는 상태임.**

  | 후보 깊이 | Recall@10 | nDCG@10 | 쓸모있는 논문 1편 이상 |
  |---|---|---|---|
  | **100** | 0.566 | **0.514** | **93.4%** |
  | 150 | 0.566 | 0.496 | - |
  | 200 | 0.569 | 0.487 | - |
  | 300 | 0.583 (+0.017, p=0.214) | 0.478 (**-0.036, p<0.001**) | 89.9% |

  후보가 늘면 재정렬기가 엉뚱한 논문을 상위로 올리는 일도 함께 늘기 때문임.
- **해결:** 서비스와 평가를 **둘 다 깊이 100** 으로 맞췄음. `app.py` 의 `DEPTH_LOCAL` 과
  `RERANK_DEPTH` 를 100 으로 바꾸고, 그 근거 표를 코드 주석에 남겼음.
- **교훈:** 후보 상한은 성능의 천장이지 성능이 아님. **상한이 올랐다는 이유만으로 깊이를
  늘리면 안 됨.** 재정렬기의 정밀도가 따라오는지 함께 재야 함.

## #39 서비스가 arXiv 채널 후보를 전부 버리고 있었음 🟢 (2026-08-16 해결)

- **발생:** 2026-08-16, `app.py` 를 실제로 실행해 확인
- **문제점:** 후보를 합치는 코드가 이렇게 돼 있음.

  ```python
  candidates = merge_channels(out["local_hits"], out["arxiv_hits"])[:RERANK_DEPTH]
  ```

  `DEPTH_LOCAL = 300`, `RERANK_DEPTH = 300` 이라 **로컬 후보만으로 300칸이 다 참.**
  실제로 실행해 보니 재정렬에 들어간 300편 중 arXiv 채널에서 온 것은 **0편**이었음.
- **딸린 문제 둘:**
  1. **순위 합치기(RRF)를 서비스에서 쓰지 않음.** `ranking.rrf_fuse` 는 평가에서만 호출됨.
     그런데 PLAN·MODULE_SPECIFICATION·README·`app.py` 설명글이 모두 RRF 로 합친다고 적고
     있었음. 문서와 코드가 어긋나 있었음.
  2. **학습한 변환기(dpo)가 결과에 영향을 주지 못함.** 변환기 결과물은 arXiv 검색어뿐인데
     그 채널이 버려지므로, 매 검색마다 호출되고 GPU 8.64GB 를 차지하면서 결과에는 기여가 없음.
- **영향:** 보고서에 적힌 "지금 서비스 구성 0.570"은 서비스의 성능이 아님. 실제 서비스는
  번역 + 로컬 단독(깊이 300)에 가까운 구성이고, 그 구성은 아직 제대로 측정된 적이 없음.
  응답 시간 약 5.1초와 GPU 8.64GB 가 결과에 영향 없는 단계에 쓰이고 있음.
- **어떤 비율로 섞어야 하는지 재 봤음** (개발용 348문항, 로컬 300 + arXiv 100, 재정렬 300):

  > **2026-08-17 보류.** 아래 nDCG 값은 #42 의 영향을 가장 크게 받는 자리임 —
  > **arXiv 채널에만 있는 후보는 1000편 중 1편만 판정을 받았음(0.001).** 나머지는 관련도와
  > 무관하게 0점으로 세어졌음. Recall 근거는 전부 판정 불가였으므로 이 결정도 근거가 없음.
  > 다만 결정 자체는 유지함 — 근거가 '정확도' 가 아니라 '최신 논문을 따로 보여준다' 이기 때문임.

  | 로컬 대 arXiv 가중치 | Recall@10 | nDCG@10 |
  |---|---|---|
  | 안 섞음 | 0.583 | **0.478** |
  | 1 대 1 | 0.578 (p=0.665) | 0.437 (**-0.041, p<0.001**) |
  | 2 대 1 | 0.583 (p=1.000) | 0.438 (**-0.040, p<0.001**) |
  | 3 대 1 | 0.589 (p=0.537) | 0.444 (**-0.034, p<0.001**) |

  **로컬 비중을 올릴수록 손해가 줄지만 손익분기점을 넘지 못함.** Recall 이득은 전부 판정
  불가임. 추세의 종점이 "arXiv 비중 0" 이므로 섞지 않는 것이 맞음.

- **해결 (2026-08-16):**
  1. **arXiv 결과를 추천 목록에 섞지 않고 "최신 논문" 칸으로 따로 보여줌.** 호출은 남김 -
     arXiv 채널의 가치는 정확도가 아니라 색인에 없는 최신 논문이고, 그 가치는 이 평가셋으로
     잴 수 없기 때문임(정답 논문이 전부 색인 안에 있음).
  2. 로컬 채널은 **검색어 2개(번역 + 가상 초록)로 두 번 검색해 순위 합치기(RRF)로 합침.**
     서비스가 처음으로 RRF 를 실제로 쓰게 됐음.
  3. **학습한 변환기(dpo)는 arXiv 를 켤 때만 호출함.** 로컬 검색에는 쓸 곳이 없음.
     끄면 GPU 8.64GB 와 약 3.5초가 통째로 사라짐.
  4. 추천 이유 생성은 dpo 가 없을 때 Ollama 의 qwen3:4b 를 씀(이미 떠 있는 모델).
  5. 결과: 워밍업 후 응답 시간 21.1초 -> **15.1초**, 시험용 Recall@10 0.617 / nDCG 0.521.
- **같은 종류의 사고가 세 번째임**(#10, #13). 재발 방지: 서비스 경로를 바꾸면 그날 평가
  인자도 같이 바꾸고, 두 경로가 같은 조건인지 확인하는 것을 마무리 절차에 넣음.

## #37 쿼리 변환기와 arXiv 채널이 새 평가셋에서 정당화되지 않음 🔴

- **발생:** 2026-08-14, 새 평가셋(v2)으로 기준선을 다시 재면서
- **측정 (시험용 342문항, 다른 조건을 전부 고정):**

  > **2026-08-17 보류.** 아래 nDCG 값은 #42 의 영향을 받음. 변환기(dpo)는 arXiv 채널의
  > 검색어를 바꾸므로 상위 10편에 들어오는 논문의 구성이 달라지고, 그만큼 판정 보유율이
  > 달라짐(passthrough 0.413 대 dpo 0.375). **"변환기가 만족도를 해친다" 는 판정 불가임.**
  > Recall 쪽 결론(+0.018, p=0.101 판정 불가)은 그대로 유효함.

  | 비교 | Recall@10 | nDCG@10 |
  |---|---|---|
  | 변환기 있음 vs 없음 | +0.018 (p=0.101, 판정 불가) | −0.016 (p=0.008, 나빠짐) |
  | 두 채널 vs 로컬 단독 | +0.018 (p=0.101, 판정 불가) | −0.043 (p<0.001, 나빠짐) |

  개발용에서 겨우 유의미했던 Recall 이득(+0.026, p=0.046)이 **시험용에서 재현되지 않았음.**
  만족도 손해는 양쪽에서 모두 재현됐음. 옛 평가셋의 같은 결론(+0.003, p=0.889 — #29)과 일치함.
- **층별로 보면 더 나쁨:** 변환기가 돕는 곳은 한국어(+0.047, p=0.006)와 이미 가장 잘 되는
  대학원생 층(+0.053, p=0.003) 둘뿐임. **전문 용어를 못 쓰는 층(medium·hard)에서는 Recall 이
  안 오르고 만족도만 확실히 떨어짐.** 이 프로젝트의 전제가 "비전문가를 돕는다"인데
  변환기는 전문가 층을 돕고 비전문가 층을 해침.
- **왜 이렇게 됐는지는 설명이 됨:** 변환기는 "정답 논문이 몇 등인가"만 보상으로 학습했음.
  Recall 이 조금이라도 오른 것은 학습 목표 그 자체이고, 만족도는 목표에 들어간 적이 없음.
- **후속으로 답이 나온 부분:** 변환의 방향을 바꾸니 이득이 나왔음. arXiv 문법을 만드는 대신
  **한국어를 영어로 옮기기만 해서 로컬 의미 검색에 넣으니** 한국어 Recall@10 이 0.456 →
  0.567(+0.111, p=0.001)로 올랐음. 학습 0회, 프롬프트 한 줄임.
  → 교훈은 "변환이 쓸모없다"가 아니라 **"변환 결과를 어느 채널에 넣느냐가 전부였음"** 임.
- **남은 결정:** 위 #39 와 함께 판단해야 함. arXiv 채널의 남은 근거는 정확도가 아니라
  최신성·범위인데, **이 평가로는 그 가치를 구조적으로 잴 수 없음**(평가셋 정답 논문이 전부
  로컬 색인 안에 있음). 졸업작품 보고서에는 이 실측을 그대로 싣는 것이 좋음 — 방향이 바뀐
  이유를 숫자로 설명할 수 있는 것이 강점임.

## #14 단일 정답 Recall 이 사용자 만족도와 다른 것을 잼 🟡

- **문제점:** 한 질문에 맞는 논문이 실제로 여러 편인데 정답으로 인정하는 것은 1편뿐임.
  그래서 완벽한 검색기도 그 1편을 상위 10에 못 올릴 수 있음(이론적 상한).
- **대응:** 등급 정답지를 만들어 nDCG@10 을 함께 봄. 두 지표가 갈리면 그것은 시스템의
  실패가 아니라 지표의 성격 차이임. **완전히 해소되지는 않는 한계이므로 계속 함께 보고함.**
- **2026-08-07 에 함께 적어 뒀던 것 (2026-08-16 압축에서 잘렸다가 2026-08-17 되살림):**

  > 그런데 그 등급 평가는 **로컬 코퍼스 경로에서만** 했고, 지금 유일한 공식 기준인
  > arXiv 실시간 경로에서는 한 번도 하지 않았다. 근본 해결은 판정기를 **arXiv 실시간
  > 결과에 적용**해 등급 정답지를 만들고 NDCG@10 을 함께 재는 것. **미착수.**

  이 미착수 항목이 실제로 결론을 흔든다는 것을 2026-08-17 에 확인했음. **-> #42 로 이어짐.**
  #14 가 "지표가 무엇을 재는가" 의 문제라면, #42 는 "그 지표를 계산할 자료가 모자란가" 의
  문제임. 둘은 다른 이야기이므로 따로 둠.

## #11 화면이 정신없어 사용자가 흐름을 따라가기 어려움 🟢 (2026-08-14 해결)

검색 전에는 입력창 하나만 보이고, 검색하면 "무엇을 어떻게 알아들었는지" 카드 뒤에 결과
목록 하나만 나오게 바꿨음. 옛 화면은 검색된 논문과 최종 추천을 따로 뿌려 같은 논문이 두 번
나왔고 어느 목록을 봐야 하는지 알 수 없었음.

---

# 2. 규칙으로 남은 것 (해결됐지만 계속 지켜야 함)

## #23 실험 설계 점검표 — 실험 전에 읽을 것

2026-08-09~10 이틀 동안 방법론 실수를 다섯 번 저질렀고 그중 둘은 잘못된 결론을 보고하는
데까지 갔음. 원인은 하나임 — **검증 설계를 대충 하고 결과를 먼저 믿었음.**

1. **정답이 시스템에 새어들어가지 않는가.** 로컬 코퍼스를 쓰는 모든 부품(용어 조회·재정렬
   후보·학습 라벨)은 정답 논문을 제외했는지 명시적으로 확인함.
2. **측정 지표가 개입과 같은 축인가.** "무엇을 바꿨고 무엇이 변할 것으로 기대하는가"를 먼저
   적고, 그 변화를 잡아내는 지표인지 확인함.
3. **비교 대상의 문항 집합이 같은가.** 결과 0건·오류 문항을 실패로 세지 않으면 기준선이
   부풀려짐(그렇게 재면 0.257 이 0.467 로 보임).
4. **자원이 충분한가.** GPU 메모리가 모자라면 모델이 조용히 CPU 로 밀려나 품질이 떨어짐.
   실행 기록에 `meta device`, `offloaded` 경고가 없는지 확인함.
5. **개수를 억지로 채우지 않는가.** 자격 미달이면 개수가 줄어드는 것이 맞음.
6. **결과가 예상과 맞으면 더 의심함.** 부풀려진 두 수치는 모두 기대한 방향이라 그냥 믿었음.

## #25 시험용 평가셋은 확정 판정에만 씀

옛 학습용 300문항은 변환기 학습에 이미 소모돼 있었고(300/300 일치), 옛 시험용도 설정 선택에
반복해 쓰여 시험지로서 죽었음. 그래서 평가셋 v2 를 새로 만들었음.
**개발용에서 탐색하고 시험용에서는 확정 판정만 함.** 이 규칙을 어기면 남은 깨끗한 자료가 없음.

## #28 색인과 코퍼스의 짝을 반드시 확인함

짝이 안 맞으면 파일 위치가 엉뚱한 줄에 떨어지는데 **JSON 해석은 그대로 성공함.** 오류도
안 나고 결과도 그럴듯한 채로 다른 논문의 제목·초록이 나옴. 크기 지문 + 표본 대조로 막고
단위 테스트로 고정했음.

## #27 논문 번호에서 버전을 뗄 때 `split("v")[0]` 을 쓰면 안 됨

`solv-int/9611001v1` 이 `sol` 로 잘림. 끝에 붙은 `v숫자` 만 정규식으로 뗌. 통일하지 않으면
두 채널이 함께 찾아낸 논문일수록 중복으로 남아 손해를 봄.

## #26 · #31 상한은 '재정렬이 실제로 본 후보'로 잼

'채널 합집합'과 '재정렬이 실제로 본 후보'는 다른 집합임. 합집합을 상한이라고 부르면
합치는 단계에서 흘린 몫까지 재정렬 탓으로 넘어가 처방이 뒤바뀜. 두 번 틀린 자리임.
보고서가 ① 채널 합집합 ② 재정렬이 본 후보 ③ 최종값을 셋 다 찍음.

## #38 바깥 세계를 부르는 곳에는 시간 제한을 걸음

`arxiv` 패키지는 안에서 시간 제한을 걸지 않아, 연결이 끊기지도 응답이 오지도 않으면 영원히
기다림. 실제로 측정이 75분 멈춰 있었고 오류도 기록도 남지 않았음.
**지수 백오프 재시도는 이 상황에 도움이 안 됨** — 재시도는 요청이 실패해야 도는데 매달린
요청은 실패하지 않기 때문임. 소켓 시간 제한 90초를 걸어 해결했음.

## #43 미해결 이슈의 본문은 압축하지 않음

2026-08-16 에 이 문서를 1,169줄에서 압축하면서 #14 의 절반(등급 평가가 로컬 경로에서만
됐다는 미착수 항목)이 사라졌고, 그 사이에 그 지표로 결정 두 건을 냈음(#39, #40).
**한 줄로 줄이는 것은 🟢 · ⚪ 만.** 미해결(🔴 · 🟡) 이슈의 본문, 특히 "미착수" 라고 적힌
항목은 그것이 곧 다음에 할 일이므로 지우지 않음.

## #36 파일을 늘리지 않음

코드 파일이 63개, `data/eval/` 이 17개까지 늘어 무엇이 진짜인지 알 수 없는 상태가 됐었음.
규칙 셋: ⓐ 디렉터리마다 코드 파일 4개 이하 ⓑ `_v2` 같은 버전 접미사로 파일을 늘리지 않음
ⓒ 폐기 판정을 내렸으면 그날 코드를 지움.

## #35 무거운 실행 전에 의존성부터 확인함

실행 환경에서 패키지 9개가 사라져 있어 평가와 서비스가 아예 안 도는 상태였음. 문서만 봐서는
알 수 없고 무거운 실행을 시작해 몇 분 뒤에야 실패함.
(2026-08-16 재확인: 실행에 필요한 패키지는 전부 있었고 `pytest` 만 빠져 있어 설치했음.
단위 테스트 39개 통과함.)

## #34 평가셋 생성기와 심판 모델을 다른 계열로 가름

옛 등급 정답지는 질문을 만든 모델이 자기 질문을 자기가 채점한 것이었음. 지금은 생성기
gpt-5.4, 심판 gpt-4.1-mini 로 고정했음.

## #32 난이도 축은 검색 방식과 무관하게 정의함

옛 평가셋의 hard 는 "제목의 어순을 바꾼 것"이라 단어 일치 검색에는 어렵고 의미 검색에는
가장 쉬운 형태였음. 그래서 hard 가 이론적 상한(0.897)을 넘는 0.970 이 나왔음.
게다가 누수 감사기가 한글을 한 글자도 세지 않아 제목을 통째로 번역한 문항이 통과해 있었음.
새 평가셋은 난이도를 **"묻는 사람이 얼마나 아는가"** 로 재정의했고, 감사는 한국어를 영어로
옮긴 뒤 비교함.

## #12 · #9 arXiv 호출 규칙

요청당 3초 간격, 단일 연결을 지킴. 안 지키면 429(요청 과다)·503(과부하)으로 평가 문항이
무더기로 유실됨. 한 번의 사용자 검색이 arXiv 를 3회 부르던 것을 1회로 줄였음.

## #10 · #13 평가 경로와 서비스 경로를 같게 만듦

로컬 평가 0.74 와 실제 서비스 0.15 의 큰 격차를 겪었음. 서비스가 실제로 쓰는 구성으로 재지
않으면 잰 값이 아무 의미가 없음. **이 문제가 #39, #40 으로 다시 나타났음.**

---

# 3. 해결·폐기된 나머지 이슈 (한 줄 요약)

원문은 git 기록(커밋 `803b37a` 시점의 이 파일)에 있음.

| 번호 | 제목 | 결말 |
|---|---|---|
| #33 | 검색어 용어 손질(term_repair) | ⚪ 모든 K 에서 성능이 떨어져 폐기. 코드 삭제 |
| #30 | 기준선 0.447 이 산술적으로 불가능한 값 | 🟢 오기였음. 0.427 로 정정 |
| #29 | "두 채널을 합쳐서 올랐다"는 잘못된 공로 배분 | 🟢 올린 것은 로컬 의미 색인 하나였음(+0.003, p=0.889) |
| #24 | 후보 확보율이 천장이었음 | 🟢 로컬 의미 검색 채널을 추가해 해결 |
| #22 | 어휘 조회 평가에 정답 논문이 새어들어감 | 🟢 편향 −0.073. 기능 자체를 폐기하고 코드 삭제 |
| #21 | 규모를 키우는 여섯 가지 시도가 전부 막힘 | ⚪ 원인이 '후보 확보 천장' 하나로 수렴(#24) |
| #20 | 폐기된 가설 두 개 | ⚪ 실측으로 반증 |
| #19 | 어휘 조회 — 실제 논문에서 용어 가져오기 | 🔴 결론 철회(#22) |
| #18 | 교차 인코더 재정렬 도입 | 🟢 0.343 → 0.467. 지금도 유효한 기법 |
| #17 | 정답 논문에서 라벨을 뽑아 재학습 | 🔴 실패. 처음 보는 논문의 고유 용어는 유추 불가 |
| #16 | 변환기에 arXiv 어휘를 학습시키는 방향 | 🟡 방향은 맞았으나 단독으로는 목표 미달 |
| #15 | 평가셋 hard 난이도의 정답 누수 | 🟡 #32 로 확대돼 평가셋 폐기 |
| #8 | arXiv 오류가 "결과 없음"으로 가려짐 | 🟢 오류와 0건을 구분해 기록 |
| #7 | 쿼리 변환이 arXiv 키워드와의 차이를 못 메움 | 🟡 로컬 의미 검색 도입으로 우회 |
| #6 | 특정 유명 논문을 설명으로 찾기 | 🟢 `paper_resolver` 로 따로 처리 |
| #5 | 웹 앱이 로컬 코퍼스를 검색하고 정확한 질문을 변환기가 뭉갬 | 🟢 실시간 검색으로 전환 |
| #4 | 만족도 판정 표본이 무작위가 아니어서 언어 편향 | ⚪ 이후 무작위 표본으로 교체 |
| #3 | 자유 텍스트 기준선에서 모델 사고 과정이 출력에 새어나옴 | 🟢 출력 형식 강제로 해결 |
| #2 | 계층 변환 후 의미 검색에서 영어 질문 성능 하락 | 🟢 원본 질문을 함께 쓰도록 수정 |
| #1 | 계층 변환기가 입력을 무시하고 예시를 그대로 복사 | 🟢 JSON 형식 강제로 해결 |
