# 파이썬
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 환경 변수
.env
data/API_KEY.env

# 논문 코퍼스와 임베딩 색인 (크고, 다시 만들 수 있음)
data/corpus/
data/embeddings/
*.faiss

# 학습된 모델 가중치
models/

# arXiv 검색 결과 캐시 (다시 만들 수 있고 계속 커짐)
data/cache/

# runs/ = 실행하면서 나오는 임시 실험 결과
# results/ = 보고서가 인용하는 확정 결과 -> 커밋해서 재현할 수 있게 둔다
runs/
logs/

# 학습 쌍 (크고, build_*_pairs.py 로 다시 만들 수 있음)
data/training/train_retriever.jsonl
data/training/val_retriever.jsonl
data/training/train_reranker.jsonl
data/training/val_reranker.jsonl

# OS, 에디터, 개발 도구
.DS_Store
.idea/
.vscode/
.pytest_cache/

# 등급 판정용 후보 풀 (실행 결과로 다시 만들 수 있고, 초록이 들어 있어 큼)
data/eval/pool_*.jsonl
