# 파이썬
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 환경 변수
.env

# 논문 원본 코퍼스, 임베딩, 검색 결과
data/corpus/
data/embeddings/
data/API_KEY.env

# runs/ = 굴러가며 나오는 임시 실험 결과 (버려도 됨)
# results/ = 보고서에 인용하는 확정 결과 → 커밋해서 재현·검증 가능하게 둔다
runs/
*.faiss

# OS / 에디터 / 개발 도구 설정
.DS_Store
.idea/
.vscode/
.pytest_cache/
# 학습된 모델 가중치 — git이 아니라 HuggingFace Hub로 배포
models/

# arXiv 검색 결과 캐시 — 재생성 가능하고 계속 커진다
data/cache/

logs/

data/training/prev_reranker_pairs/

# 검색 모델 학습 쌍
data/training/train_retriever.jsonl
data/training/val_retriever.jsonl

# 재정렬기 학습 쌍
data/training/train_reranker.jsonl
data/training/val_reranker.jsonl
