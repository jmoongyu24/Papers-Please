# Papers, Please 의존성
#
# 설치:
#   pip install -r requirements.txt
#
# GPU 를 쓰려면 torch 를 먼저 CUDA 빌드로 설치할 것:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126

# ── 서비스 실행 ───────────────────────────────────────────────────────────
streamlit>=1.60                 # 웹 화면
arxiv==2.4.1                    # arXiv API 실시간 검색
numpy>=2.0                      # 임베딩 연산, 지표 계산
ollama>=0.6                     # 로컬 Qwen3-4B 서빙 (별도로 Ollama 데몬 필요)
sentence-transformers>=5.0      # bge-m3 임베딩, bge-reranker 교차 인코더
torch>=2.5

# ── 학습 (파인튜닝할 때만) ────────────────────────────────────────────────
transformers>=5.0
peft>=0.19                      # 보조 행렬(LoRA) 학습
trl>=1.9                        # 지도 파인튜닝, 선호 학습 트레이너
datasets>=4.0
accelerate>=1.0
bitsandbytes>=0.43              # 4비트 양자화 (메모리가 모자랄 때만)

# ── 평가셋 만들기 (오프라인 전용, 서비스 실행에는 불필요) ─────────────────
openai>=2.0                     # 질문 생성, 등급 판정

# ── 개발 ─────────────────────────────────────────────────────────────────
pytest>=8.0
