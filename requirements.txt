# Papers, Please 의존성
#
# 파이썬 3.11 이상. 개발과 검증은 3.13.9 에서 했다.
#
# 설치:
#   pip install -r requirements.txt
#
# GPU 를 쓰려면 torch 를 먼저 CUDA 빌드로 설치할 것:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
#
# 검증된 조합 (2026-09 기준):
#   torch 2.10.0 · transformers 5.15.0 · sentence-transformers 5.7.0
#   peft 0.20.0 · trl 1.10.0 · datasets 5.0.1 · accelerate 1.14.0
#   numpy 2.3.5 · arxiv 4.0.1 · ollama 0.6.2 · streamlit 1.51.0 · openai 2.33.0

# ── 서비스 실행 ───────────────────────────────────────────────────────────
streamlit>=1.50                 # 웹 화면
arxiv>=4.0                      # arXiv API 실시간 검색
numpy>=2.0                      # 임베딩 연산, 지표 계산
ollama>=0.6                     # 로컬 Qwen3-4B 호출 (Ollama 데몬은 따로 설치)
sentence-transformers>=5.0      # bge-m3 임베딩, bge-reranker 교차 인코더
transformers>=5.0               # 위 패키지가 쓰는 토크나이저와 모델
torch>=2.5

# ── 학습 (파인튜닝할 때만) ────────────────────────────────────────────────
peft>=0.20                      # 보조 행렬(LoRA) 학습
trl>=1.10                       # 지도 파인튜닝, 선호 학습 트레이너
datasets>=4.0
accelerate>=1.0                 # 위 트레이너가 요구함

# ── 평가셋 만들기 (오프라인 전용, 서비스 실행에는 불필요) ─────────────────
openai>=2.0                     # 질문 생성, 관련도 등급 판정

# ── 선택 ──────────────────────────────────────────────────────────────────
# bitsandbytes>=0.43            # `train sft --use-4bit` 를 쓸 때만 필요하다.
#                               # 기본 학습은 양자화 없이 bf16 으로 하므로 없어도 된다.
